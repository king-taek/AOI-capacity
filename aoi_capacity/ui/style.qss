/* AOI Capacity — Qt 크롬 스타일. 토큰(달러 기호 자리표시자)은 theme.py 가 template.html 의 :root 값으로 채운다.
   규칙: 전역 QWidget 에는 배경을 칠하지 않는다(면은 역할별로만). */

QWidget { color: $ink; font-family: $font_body; font-size: 14px; }
QMainWindow, QDialog { background: $bg; }
QToolTip { background: $raise_; color: $ink; border: 1px solid $line_2; padding: 6px 8px; border-radius: 6px; }

/* 상단 바 · 사이드바 */
QWidget[role="top"] { background: $surface; border-bottom: 1px solid $line; }
QWidget[role="side"] { background: $surface; border-right: 1px solid $line; }
QLabel[role="brand"] { font-family: $font_display; font-size: 17px; font-weight: 700; }
QLabel[role="logo"] { background: $accent; border-radius: 8px; }
QLabel[role="eyebrow"] { font-size: 11px; font-weight: 600; color: $ink_3; letter-spacing: 1px; }
QLabel[role="pill"] { background: $accent_soft; color: $accent_2; border-radius: 10px; padding: 2px 10px; font-size: 12px; font-weight: 600; }
QLabel[role="pill"][kind="live"] { background: $good_soft; color: $good_ink; }
QLabel[role="pill"][kind="stale"] { background: $warn_soft; color: $warn_ink; }
QLabel[role="pill"][kind="bad"] { background: $crit_soft; color: $crit_ink; }

QFrame[role="navItem"] { background: transparent; border-radius: 8px; border-left: 2px solid transparent; }
QFrame[role="navItem"]:hover { background: $raise_; }
QFrame[role="navItem"][current="true"] { background: $accent_soft; border-left: 2px solid $accent; }
QLabel[role="navTitle"] { font-size: 14px; color: $ink_2; }
QFrame[role="navItem"][current="true"] QLabel[role="navTitle"] { color: $accent_2; font-weight: 600; }
QLabel[role="navSub"] { font-size: 11px; color: $ink_3; }
QLabel[role="foot"] { font-size: 11.5px; color: $ink_3; }

/* 페이지 */
QLabel[role="h2"] { font-family: $font_display; font-size: 20px; font-weight: 700; }
QLabel[role="help"] { color: $ink_2; font-size: 13px; }
QLabel[role="muted"] { color: $ink_3; font-size: 12.5px; }
QLabel[role="mono"] { font-family: $font_mono; font-size: 12.5px; }
QFrame[role="card"] { background: $surface; border: 1px solid $line; border-radius: 14px; }
QFrame[role="card"] QLabel[role="cardTitle"] { font-family: $font_display; font-size: 15px; font-weight: 600; }
QFrame[role="notice"] { background: $accent_soft; border-radius: 10px; }
QFrame[role="notice"] QLabel { color: $accent_2; }

/* 버튼 */
QPushButton { background: $surface; color: $ink; border: 1px solid $line_2; border-radius: 8px; padding: 6px 12px; font-size: 13px; font-weight: 500; }
QPushButton:hover { background: $raise_; }
QPushButton:pressed { background: $accent_soft; }
QPushButton:disabled { color: $ink_3; border-color: $line; }
QPushButton[role="primary"] { background: $accent; color: $accent_ink; border-color: $accent; font-weight: 600; }
QPushButton[role="primary"]:hover { background: $accent_2; border-color: $accent_2; }
QPushButton[role="primary"]:disabled { background: $raise_; color: $ink_3; border-color: $line; }
QPushButton[role="ghost"] { background: transparent; border-color: transparent; color: $ink_2; }
QPushButton[role="ghost"]:hover { background: $raise_; color: $ink; }
QPushButton[role="danger"] { background: $crit_soft; color: $crit_ink; border-color: transparent; }

/* 입력 */
QLineEdit, QSpinBox, QComboBox, QPlainTextEdit, QTextEdit { background: $surface; color: $ink; border: 1px solid $line_2; border-radius: 8px; padding: 5px 8px; selection-background-color: $accent_soft; selection-color: $ink; }
QLineEdit:focus, QSpinBox:focus, QComboBox:focus { border-color: $accent; }
QLineEdit:disabled, QSpinBox:disabled { color: $ink_3; }
QLineEdit[role="mono"] { font-family: $font_mono; }
QSpinBox::up-button, QSpinBox::down-button { width: 14px; border: none; background: transparent; }
QComboBox::drop-down { border: none; width: 22px; }
QComboBox QAbstractItemView { background: $surface; border: 1px solid $line_2; selection-background-color: $accent_soft; }
QCheckBox { spacing: 8px; color: $ink_2; }
QCheckBox::indicator { width: 16px; height: 16px; border: 1px solid $line_2; border-radius: 4px; background: $surface; }
QCheckBox::indicator:checked { background: $accent; border-color: $accent; }

/* 표 */
QTableWidget, QTableView { background: $surface; alternate-background-color: $surface_2; gridline-color: $line; border: 1px solid $line; border-radius: 10px; selection-background-color: $accent_soft; selection-color: $ink; }
QTableWidget::item { padding: 4px 8px; }
QHeaderView::section { background: $surface; color: $ink_3; font-size: 11.5px; font-weight: 600; padding: 6px 8px; border: none; border-bottom: 1px solid $line; }
QTableCornerButton::section { background: $surface; border: none; }

/* 진행 · 콘솔 */
QProgressBar { background: $raise_; border: none; border-radius: 3px; height: 6px; text-align: center; color: transparent; }
QProgressBar::chunk { background: $accent; border-radius: 3px; }
QPlainTextEdit[role="console"] { background: $console_bg; color: $console_ink; font-family: $font_mono; font-size: 12px; border: 1px solid $line; border-radius: 8px; }

/* 스크롤바 */
QScrollBar:vertical { background: transparent; width: 10px; margin: 0; }
QScrollBar::handle:vertical { background: $line_2; border-radius: 5px; min-height: 24px; }
QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical { height: 0; }
QScrollBar:horizontal { background: transparent; height: 10px; margin: 0; }
QScrollBar::handle:horizontal { background: $line_2; border-radius: 5px; min-width: 24px; }
QScrollBar::add-line:horizontal, QScrollBar::sub-line:horizontal { width: 0; }
QScrollArea { border: none; background: transparent; }
QScrollArea > QWidget > QWidget { background: transparent; }

/* 시트(팝업) · 로딩 카드 */
QFrame[role="sheet"] { background: $surface; border: 1px solid $line_2; border-radius: 14px; }
QLabel[role="sheetTitle"] { font-family: $font_display; font-size: 16px; font-weight: 600; }
QLabel[role="sheetBody"] { color: $ink_2; font-size: 13.5px; }
QFrame[role="loadCard"] { background: $surface; border: 1px solid $line_2; border-radius: 16px; }
QLabel[role="loadTitle"] { font-family: $font_display; font-size: 17px; font-weight: 600; }
QLabel[role="loadPhase"] { font-size: 13.5px; color: $ink; }
QLabel[role="loadDetail"] { font-family: $font_mono; font-size: 11.5px; color: $ink_3; }
QLabel[role="loadCount"] { font-family: $font_display; font-size: 22px; font-weight: 600; }
QLabel[role="legend"] { font-size: 11.5px; color: $ink_2; }
