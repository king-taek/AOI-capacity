# 진행상황 기록 보관 (2026-09-13 ~ 2026-09-19)

`진행상황.md` 가 9/20 에 80KB 로 불어 진입부를 줄이면서(S08, D62) 완료·해소된 항목을 **원문 그대로** 여기로 옮겼다. 결정 번호(D01~)는 옮기지 않았다 — 결정은 `진행상황.md` 에 그대로 있다.
현재 상태는 언제나 `진행상황.md` 가 기준이다. 이 파일은 고치지 않는다.

## 다음 할 일에서 옮긴 완료 묶음

0000. **D47 이식 — 재설계 화면을 `template.html` 로** — **9/20 완료**(한 커밋, 원장 `Phase D47`):
   - [x] P0 기준선: 지금 template 의 30일치 수치(measure) 보관 · 새 모델의 기준 수치는 `make_aoi_data.js` 출력(장비-일 `r/x/t/d/s/e`)으로 잡는다
   - [x] P1 모델: `make_aoi_data.js` 의 [원본] 블록을 template JS 로 **그대로** 옮겨 embedded JSON 에서 열 때마다 계산(`buildModel`). D48 제품 유지 2건만 바꾼다 —
         Error 후 대기 = 다음 기록까지(cap 없음, 그날 마지막이면 관측 종료·자정 중 이른 것까지, D44) · 분모 = 오늘만 00:00~마지막 스캔(D17). `day` = 수집일. Node 하네스 테스트로 JS 출력 = 스크립트 출력 확인(두 상수 외 동일)
   - [x] P2 화면: `AOI-Dashboard.dc.html` 의 `renderVals` 로직을 그대로 두고 sc-for/sc-if 마크업만 순수 JS 렌더로 변환(React·support.js 없이). 가동률·Error·추이·리포트 4탭 + 장비/Error/유형/Job 팝업, 라이트 단일(`:root` 토큰은 남겨 `ui/theme.py` 가 읽게)
   - [x] P3 유지할 제품 기능: BatchReport 열기(팝업의 Report 줄 → 새 탭, `reportUrl`) · 수집 상태 칩(unreachable/partial) · 범위 밖 '수집 안 함' · 사본 저장. 뺄 것: 데이터 품질 목록·자재 이력·장비 비교 열 선택·설정(분류 기준표)·저하 사유 차트(D47 구조에 없음)
   - [x] P4 테스트: `test_dashboard_js.py`(D30~D44 기반 66개) 를 새 모델 테스트로 교체, `test_template_contract.py`·`test_theme.py`·`test_status_mapping.py`(CR 규칙 = `_CAUSE_RULES`) 갱신
   - [x] P5 문서: CLAUDE.md 규칙 5(추정)·D30 용어·D31/32 재스캔·D36 Job·lotKey·D44 를 D48 로 개정, README 화면 설명 교체

00. **9/18 실행 계획(검토·보완본 JSON, D36~D46) — phase 순서대로 커밋한다.** 각 phase 뒤 `dev/tools/measure.py` 로 전후를 재고 원장에 적는다.
   - [x] phase 0 기준선(샘플 보관 · 헬퍼 · 하네스 시계 · 원장)
   - [x] phase 1 D43 원인/결과 축 · 213문구 전수 매핑 · Python/JS 동일 규칙 · 옛 17열 payload 어댑터
   - [x] phase 2 D41 중단을 Report 의 PASS 유무로 나눔(Error 중단 / 가동 중단)
   - [x] phase 3 D37 같은 INI 시각의 시간 소유권(엄격 포함 Report 하나 = 소유자)
   - [x] phase 4 D38 부분 실패 배치의 Slot Error 를 Report 당 1건으로 · 실패 배치 대표 원인의 결정적 선택
   - [x] phase 5 D39·D36·D42 자재 키 정규화 · Job 원문 = 공정 · override 1건 · Rework+재스캔
   - [x] phase 6 D40 기준시각 = 수집 시각
   - [x] phase 7 첫 화면 숫자·대비·단위(V01~V03·V08) + 데이터 품질 목록
   - [x] phase 8 Error 분석 화면(기간별·장비별·Job별 · 필터 · 세부 설정 · 저장된 보기) — 브라우저 실측 완료(원장 phase 8-②③)
   - phase 9(P1, 다음 세션): 고급 품질 탐색·원인 사전 편집 · 원인 Pareto·메시지 fingerprint·근거 포함 내보내기 · 장비×날짜 행렬 · 추가 인터랙션 · 소스 모듈 분리(배포는 HTML 한 장)
   - 사용자 확인 대기: 새 화면(Error 분석 · 데이터 품질 · 핵심 카드 4개)과 D41·D37 로 달라진 수치(Error 3,319건 · 정지(추정) 증가)가 현장 감각과 맞는지. OQ1~OQ5 는 선택 검토.
   - 브라우저 실측은 headless Chromium 이다 — 실제 노트북 화면(1280·125%)에서 한 번 더 보는 것이 남았다.
0. **Codex 인계 프롬프트 검토 후 확정한 구현 묶음(사용자 결정: 세 묶음 전부 + 확대 타임라인)** — 순서대로 커밋한다.
   - [x] ① 저장 회귀(P04) · 문구 정합(P11) · Wafer/배치 분리(U13)
   - [x] ② 캐시 파서 버전 + 누락 복구(P05) · 장비별 수집 상태(P06)
   - [x] ③ 단계별 계측(P01) · INI 메모/사전 확인 제거(P02)
   - [x] ④ 타임라인 hit area(U05) · 툴팁 clamp·3줄(U06·U04) · 모션(U08)
   - [x] ⑤ 클릭 상세 패널(U04) · 표(U10) · 문구(U15) · 좁은 창 탭(U09 — 노트북 작은 창도 자주 씀)
   - [x] ⑥ 확대 타임라인(B09)
   - 보류(사용자: 한 PC 수동 수집·HTML 공유): 중앙 수집·NAS 인접 호스트·변경 목록·자동 조절(B01·B02·B03·B12), SQLite·Worker(P09).
0-1. **9/17 인계 프롬프트 구현(D30~D34) — 완료, 사용자 화면 확인 대기.** 첨부 `AOI_capacity 3.html`(30대 18,719행)로 다시 열어
   중복스캔·재스캔 건수와 사유 차트가 현장 감각과 맞는지, Q01~Q03(확인 대기) 답을 받아 규칙을 확정한다. 보류(P11 시나리오 슬라이더)는 만들지 않았다.

## 확인 대기에서 옮긴 해소 항목

- ~~RQ1~RQ7~~ → 9/20 답: 중단 D52 · Test D53 · 추정 D54 · 합치기 D55. 평균 감각(RQ7)만 미정 — 재수집 뒤 확인. (원문 기록) **RQ1~RQ7 (9/20 이식 검수 — 세션에 남긴 확인 아티팩트)**: RQ1 원인 없는 중단(`Aborted.`·PASS 없는 Report)이 새 모델에서는 Error 가 아니다(D41 사실상 폐기 — 옛 정지(추정) 1,804h → 대기 548h 의 주된 차이) ·
  RQ2 Test 판정이 Lot 원문의 `TEST` 부분 문자열(수집기 토큰 규칙과 달라 `TESTER` 도 Test) · ~~RQ3 Error 팝업의 Lot 줄 원문~~(버그로 보고 고침) ·
  RQ4 추정 장비-일에서는 실패한 배치의 구간도 '돌아간 시간' 에 들어간다(배치 시작~종료 합) · RQ5 서로 다른 종류의 구간이 겹치면 둘 다 센다(시간 소유권 없음 — 옛 U+T+E+S+R=D 불변식 없음, 분모 초과 가능) ·
  RQ6 결과 미확인(`-`)·취소(`Cancelled`) 행은 시각이 있으면 Scan 으로 센다(옛 UNCLASSIFIED 없음) · RQ7 fleet 평균 40.6% → 55.0% 의 요인 분해(추정 123 장비-일 · 3분 병합 · 중단 제외)가 현장 감각과 맞는지.
- ~~DQ4 후속~~ → 9/20 받음(`docs/design/dashboard-redesign/scripts/`). 조사 스크립트 4개는 파일로 존재한 적이 없어 결과 txt 만 남는다(디자인 세션 답변). **D47 이식 착수 가능.**
- ~~4F NOT_FOUND 원인 확인 요청~~ → 9/20 확인됨, `job_folder_variants` 로 수정. 실장비 `--full` 재수집 뒤 4층 시각 확인 비율이 오르는지 본다(다음 할 일 000).
- ~~DQ1~DQ5~~ → D47~D51 로 확정(9/20).
- **(참고) DQ1~DQ5 (9/19 디자인 handoff, 세션에 남긴 결정 페이지 HTML 참조)** — DQ1 재설계 화면을 `template.html` 에 이식할 때 규칙 충돌 9항목(시각 미확인 추정 vs 규칙 5 · Error 건수 Lot 단위 vs 사건 · 재스캔 정의 · Job 병합 vs D36 · Lot 이름 정리 vs 원문 · 대기 상한 240분 vs D44 · 분모 00~24 고정 vs 오늘 마지막 스캔 · 라이트 단일 vs 테마 토큰 · 용어 D30)을 어느 쪽으로 확정할지 ·
  DQ2 리포트의 배치시간 정의(배치 시작~종료로 적용함) 와 표본 5개 미만 회귀 생략 확인 · DQ3 4층 5대 백업 없음 → 매일 자동 수집(규칙 3 과 충돌) 허용 여부 ·
  DQ4 프로토타입 데이터(`aoi-data.json`) 추출 스크립트가 handoff 에 없음 — 이식하면 불필요, 프로토타입을 계속 쓰려면 새로 만들어야 함 · DQ5 Error 팝업의 기간 모드 날짜·Job 필터 해석 확인.

## 작업 기록 (2026-09-13 ~ 2026-09-19)

- 2026-09-19 `ce05d4e` — **디자인 프로토타입(handoff 할 일 1·2) 반영 + 저장소 편입**: `docs/design/dashboard-redesign/` 에 Claude Design handoff 를 넣고
  본체 `app/AOI-Dashboard.dc.html` 에 ① Error 상세 팝업(시안 A — Error 탭 장비 줄·유형/Job 팝업의 장비/Lot 줄에서, Job·유형 필터 칩, 가동률 팝업과 상호 이동, 날짜 이동)
  ② 리포트 탭(표기명 21개 · 기간 31/21/7 · 준비/장당 ± 회귀 표준오차 · 최빈 장수 환산 · 장비별 펼침 · 표본 5개 미만은 회귀 안 냄)을 추가.
  `dev/tools/design_bundle.py` 가 원본 번들 형식 그대로 오프라인 단일 HTML 을 만든다(Chromium 으로 클릭 경로 확인, 콘솔 오류 0). 가드 `test_design_bundle.py` 4개.
  제품 이식은 규칙 충돌 때문에 보류 — '확인 대기' DQ1~DQ5.
- 2026-09-19 `6b4a114` — **수집기: Scanresult 백업 폴더 조회 + `faults`·`scanned_dice`·`yield` 열**(디자인 handoff 의 할 일 3·4): 장비 폴더의 `Scanresult*` 를 전부 후보로 두고
  폴더 이름의 경계 날짜로 배치의 1순위 폴더를 바로 고른다(`devices.scan_dirs_of`·`backup_cutoff`, `collect.ini_roots_for`, 확인 횟수 1번 유지). 어디에도 없고 `MoveResultFlag` 만 있으면 `MOVED_ONLY`
  (누락 복구 대상, 품질 목록·요약에 분리 표시). Report 표의 Faults·Scanned Dice·Yield 를 원문 그대로 행에 싣는다(24열, `ROW_SCHEMA_VERSION` 5, 합성 행은 빈 값). 테스트 +10(빠른 전체 450 · slow 통과).
  실장비 재수집 전후 비교는 사용자 PC 에서만 가능 — '다음 할 일 000'.
- 2026-09-19 `47df58c` — **graphify 코드 지도 자동 갱신**: `dev/tools/graphify_build.py`(표준 라이브러리만 · template.html 의 인라인 JS 를 임시 파일로 빼내 168개 함수까지 그래프에 넣고 지운다)와
  `.github/workflows/graphify.yml`(main 푸시마다 실행 → 결과는 **`graphify-out` 브랜치 커밋 하나로 덮어쓰기** + Actions 산출물 30일). main 에는 커밋하지 않는다 — 업데이터가 main SHA 를 보므로 봇 커밋이 헛 업데이트를 만든다.
  가드 `test_graphify_build.py`(임시 JS 는 실패해도 안 남음 · .gitignore 에 넣으면 graphify 도 무시하므로 넣지 않음 · 워크플로가 main 에 안 쓰는지).
- 2026-09-19 `f79354e` · `c343ccc` · `fa2da9d` — **수집 창 렉 원인 수정**: 수집 화면이 열릴 때·체크박스를 누를 때·장비 저장 뒤마다 `plan_run` 이 **UI 스레드에서** 캐시 JSON 전체(30일치면 83MB)를
  읽고, 규칙 번호(`PARSER_VERSION`)가 달라진 캐시는 15만 행을 매번 재분류(저장은 안 해서 반복) — 실측 한 번에 7.3초 정지. 이제 `plan_run` 은 재분류하지 않고
  파일 도장(mtime·size)으로 요약을 메모하며(첫 1.2초, 이후 0초), 수집 화면은 `_PlanWorker`(QThread)로 조회해 창이 멈추지 않는다. 재분류는 수집 워커에서 한 번만(6.7초) 하고 저장한다. 가드 테스트 추가.
- 2026-09-18 `9266933` — **마무리**: CLAUDE.md 에 새 계약(21열 스키마·원인/결과·D41 지표 상태·D37 소유권·자재/공정 키·D40 시계·Error 분석·저장 호환)을 적고 README 에 새 화면 두 개를 추가.
  사본 저장은 화면이 합성한 slot 행을 빼고 저장(열 때 같은 규칙으로 다시 만든다) — 저장 → 재열기 전 수치 동일 실측. 테스트 425개.
- 2026-09-18 `a6248f1` — **phase 8-②③ Error 분석 화면**: 새 메뉴 'Error 분석'(좁은 창은 홈 · Error 분석 · 더보기 ▾) — 기간(전체/최근 7·30개 날짜/직접, 데이터 마지막 날짜 기준) ·
  장비 다중선택(검색 · 전체/없음/검색 결과) · Job 다중선택(원문 리터럴 검색 · 검색 결과 전체 선택 · 전체 · 해제 · Job 미확인 토글, 펼칠 때만 그림) · 지표(사건 수 / Wafer 1,000회당 / 확인된 구간 시간) ·
  요약 4카드 · 기간별|장비별|Job별 탭(같은 사건 집합) · 차트+표(정렬·n<30·cap 배지) · 근거 상세(원문 Job·Lot·Wafer·원인·결과·종류·구간·이후 공백·시간 근거·Report 열기·보기→장비 상세→복귀) ·
  세부 설정 서랍(draft 적용/취소 · 집계/포함 기록/시간 근거/표시 · 저장된 보기 4프리셋 + localStorage(폴백) · 설정 JSON 내보내기/검증 불러오기) · 장비 비교 열 선택(V04) · 겹친 막대 안내(V07).
  Chromium 실측: 탭 전환 p95 300ms(최적화 전 1.1~1.4s) · 콘솔 오류 0 · 외부 요청 0 · 1280/1440/1920 · 125~150% · 800px. `CLASS_VERSION` 9.
- 2026-09-18 `6d55179` — **phase 8-① Error 분석 모델**: 사건 canonical 인덱스(`occurrenceIndex` — wafer_timed · wafer_untimed(Batch 시작일 귀속) · failed_batch · partial_batch_slot, 안정 ID, TEST 는 진단용),
  조회 계약(`DEFAULT_Q`·`errorQuery` — 기간 프리셋은 데이터 마지막 날짜 기준, null=전체·[]=선택 없음·Job 미확인 토글, 원인 필터는 분자만, 이후 공백은 장비 전체 이력이라 Job 필터 불변).
  30일치: 사건 3,319(= nErr 합) · Wafer 시도 132,713 중 Error 2,347. 테스트 6개(세 탭 동일 집합 · 분모 · 공백 불변 · null/[] · 프리셋 · TEST 진단).
- 2026-09-18 `d98e647` — **phase 7 V01~V03·V08**: 데이터 범위 줄(파일 범위·부분일 배지·선택일·장비·Job·수집 시각), 핵심 카드 4개(평균 가동률 · 확인된 가동시간 · Error 사건 · 시간 근거 상태,
  13/30/14/12.5px, 클릭 → 사유 상세 / 품질 목록), `--ink-3` 대비 3.5→5.4(dark)·3.7→5.5(light, `theme.py` 동기), 단위(20s · — · 1,234건), Error 원문 펼치기·Job 열·복사,
  새 화면 **데이터 품질 목록**(사유 9종 → 원문 근거 행 → 보기 → 복귀, 필터 유지). Chromium 실측: 30일치 열기 3.7초 · 콘솔 오류 0 · 외부 요청 0 · 800px 가로 스크롤 없음(원장 phase 7).
- 2026-09-18 `48acc0a` — **phase 6 D40**: 화면의 '오늘' = 수집 시각(`meta.generated_iso`, `metricsRef`) — 며칠 뒤·다른 시간대 PC 에서 열어도 같은 숫자
  (30일치를 9/25 09:00 · UTC 로 열어도 전 수치 동일, 원장 phase 6). 상단·홈에 '9/18 12:59 수집 기준', 없는 옛 파일은 '열람 시각 기준(비고정)'. README·설정 도움말 갱신. `CLASS_VERSION` 8.
- 2026-09-18 `b63fca3` — **phase 5 D39·D36·D42**: 자재 키 = [정규화 Lot(글자·숫자 외 구분자 무시 · RE/RESCAN/REWORK/TEST 토큰 제외 · 대문자), 정규화 Wafer ID] JSON 튜플(Python `material_key` = JS `materialKey`, fixture 대조).
  공정 = Job 원문 정확 일치 — 같은 자재의 같은 Job 앞선 시도만 반복 후보(A→B→A 는 앞선 A 와, 빈 Job 은 잇지 않음, 간격 무관). 신뢰도: Job 일치 = 확정 · 앞선 시도 시각 미확인 = 추정 · Recipe 차이는 배지.
  사용자 확인 예외 1건(`USER_OVERRIDES`, GWM5K09-F4 AOI-11→AOI-9, 정확한 두 시도에만 · 시각 불변). Rework+재스캔은 재스캔 색 + Rework 배지. scan_type 분리기도 같은 규칙(바뀐 Lot 1종).
  30일치: 다른 장비 재스캔 20,788 → 1,119 · 확인된 첫 기록 100,476 → 121,375 · 보류 4 → 1, **가동시간·가동률 불변**(원장 phase 5). 합쳐진 원문 쌍 1,835키 목록 `material_merge_2026-09-18.tsv`(OQ1 후보 188). `CLASS_VERSION` 7.
- 2026-09-18 `5d31787` — **phase 4 D38**: 일부만 성공한 배치의 자리표시(LoadPort/Slot) 행 Error 를 **Report 당 1건 + 영향 Slot 수**(kind=slot, 시간 미확인 · Batch 범위 추정 배지,
  시간 0 기여)로 살린다. 합성 규칙 `synthesize_rows`(Python) = `synthesizeRows`(JS) — 새 수집·캐시 재분류·옛 17열 payload 가 같은 규칙, 두 번 돌려도 같다(slot 재구축 · batch 는 없을 때만 생성).
  30일치: slot 103건 + 새 batch 22건(= 후보 125 Report), Error +123건, 관계·가동률 불변(원장 phase 4). `PARSER_VERSION` 5 · `CLASS_VERSION` 6 · 행 21열(`slots`).
- 2026-09-18 `9358337` — **phase 3 D37**: 같은 INI 시각을 여러 Report 가 참조하면 그 시각을 Batch 구간에 **엄격히** 담은 Report 가 하나뿐일 때 그 Report 가 시간을 갖고,
  나머지 행은 '시간 미확인 · INI 덮어써짐'(사건·원문·이력 보존, 시간 0 기여). 하나가 아니면 소유권 보류(옛 병합, 대표 행은 결정적으로). `build` 가 2-pass(그룹 → 소유 → 발행)라 입력 순서 무관.
  Python 에 `time_basis` 열·`time_basis()`. 30일치: 묶음 413(상태 다른 259) 중 412 가 소유자 하나, 시간 잃은 행 415 — Error 구간 −10.5h → 가동, 정지(추정) −85.2h → 미가동(원장 phase 3, 전수표 `ini_ownership_2026-09-18.tsv`).
- 2026-09-18 `f14dcdb` — **phase 2 D41(D35 개정)**: 지표 상태를 한 번만 정한다(`metricState`) — 원인 없는 중단은 **같은 Report 에 PASS 가 없으면 Error 와 똑같이**
  (건수·빨간 교차 빗금·그 뒤 정지(추정)), 있으면 가동(abortRun ⊂ run, 저하 사유 아님). 건너뜀·취소·미확인에 시각이 있으면 '분류 미확인' 으로 가동에도 Error 에도 넣지 않는다.
  Report 문맥은 필터·시간 유효성 전 원천 전체. 30일치 실측: Error +517건 · Error 구간 +31.8h · 정지(추정) +753.8h(미가동에서 옮겨 감) · 가동 +7.8h(원장 phase 2). `CLASS_VERSION` 4.
- 2026-09-18 `010ef2c` — **phase 1 D43**: 상태 문구에서 원인(`cause`, 세미콜론 배열)과 종료 결과(`outcome`)를 따로 뽑는다 — 원인 규칙 16개를 전부 먼저,
  결과(PASS·USER_CANCELLED·UNKNOWN·USER_ABORT·SKIPPED·ABORTED·FAILED)는 그 뒤. `norm_status` 는 호환 필드. Python/JS 정규식이 글자까지 같고(가드),
  213문구 전수 표(`dev/samples/status_mapping_2026-09-18.tsv`, 사람이 검토)와 Python·JS 를 비교하는 테스트. 화면은 저장값 대신 원문에서 늘 다시 계산(옛 17열 파일 호환).
  실패 배치 대표 행은 결정적으로(근거 행 많은 원인 → 규칙 순 → 원문 사전순), 원인은 하위 행 합집합. `PARSER_VERSION` 3 · `CLASS_VERSION` 3 · 행 19열(`ROW_SCHEMA_VERSION` 2).
  30일치 실측: 건너뜀에 묻힌 ID 인식 실패 192행 회복, Error +27건, 정지(추정) +24.9h(원장 phase 1).
- 2026-09-18 `ddd53aa` — **phase 0 기준선 고정**: 30일치 첨부(156,109행 · 12,665 Report · 30대 · 8/19~9/18)를 `dev/samples/…_30일치.html.gz` 로 보관,
  이름 기반 열 복원 헬퍼(`dev/tests/sample_rows.py`) · 열람 시계 주입과 요약 모드가 있는 하네스(`js_harness.js`) · 집계 도구(`dev/tools/measure.py`) 를 만들고
  지금 코드의 수치를 `dev/samples/ledger_2026-09-18.md` 에 적었다(항등식 위반 0 · 관계·표시 건수는 원 계획과 같음, 분모 차이는 기준시계 명시로 설명).
  사용자 결정 D36~D46 을 표에 올리고 D35 에 '→ D41 로 개정' 을 적었다. 확인 대기는 OQ1~OQ5(비차단)로 정리.
- 2026-09-17 `9be6d0d` — 중단(Abort)을 Error 에서 뺐다(D35): Error 건수·정지(추정)에서 제외하고 '중단' 으로 따로 표시,
  분모 포함·분자 제외는 유지. 반송 실패는 Error 로 남는다. 가동률은 5일 모두 불변, Error 건수 150→93(9/14) 등.
  분류 규칙 번호 `CLASS_VERSION` 2 로.
- 2026-09-17 `e346761` — 첨부 30대 파일(`dev/samples/…_30대.html`, D26)로 재검증: 5일 총 가동시간·평균 동일, 49개 동일 시각 참조 1번 집계, 전후 비교표(행/시도/시간)와 성능 기록. 폭이 같은 Lot/조각은 조각 우선(43/43).
- 2026-09-17 `a9137c3` — 사용자 확정 5건(D30~D34): 용어·색·패턴 레지스트리(`DISPLAY_META`), 데이터 중복/시각 중복 참조/실제 반복 분리 +
  전 장비 자재 이력(`materialIndex` — 중복스캔·같은/다른 장비 재스캔·보류·확정/추정·판정 근거), 우선순위 시간 분할·건수 시작일 1회·정지(추정) 관측 종료 cap,
  가동률 저하 사유 차트→상세→이벤트→장비 이동·복귀, 반복 가동 카드(+교차표), 장비 버튼 바로 아래 상세, 반복 조각 hit·↑↓ 순환·클릭 좌표 재판정,
  설정 탭 분류 기준표. Node 실행 테스트 26개(`js_harness.js`)와 Chromium 실측·저장 재열기 비교.
- 2026-09-16 `ffa06fe` — 기본 브랜치를 `main` 으로: 자동 업데이트 폴백(`DEFAULT_BRANCH`)과 코드 받기 도구(`BRANCH`)를 `main` 으로 맞춤.
- 2026-09-16 `3709c6c` — 테스트 수 표기(325) 바로잡음. 최종 템플릿으로 저장→재열기·hit area·확대를 다시 재 확인.
- 2026-09-16 `0fe2cc2` — 타임라인 그리기를 `drawBand` 로 분리해 개요 아래에 드래그로 고른 구간(10분~6시간)을 크게 다시
  그리는 확대 패널을 둠(같은 dayMetrics 결과 — 숫자는 안 바뀐다). 1시간·4시간·◀▶·닫기.
- 2026-09-16 `e95e1c7` — 클릭·Enter 로 고정되는 상세 패널(원문 복사·INI 확인 상태·Report 목록), 표에 'Report 열기'
  버튼과 '원본 n개' 표시, 범례·설정·수집시각 문구를 짧게(한정어는 유지), 820px 미만 상단 4탭과 타임라인 가로 스크롤.
- 2026-09-16 `41e240d` — 타임라인에 포인터 층(가장 가까운 막대 ≤6px, 겹치면 좁은 쪽·같은 Lot 이면 오류 먼저,
  Tab 으로 전환), 툴팁을 3줄로 줄이고 네 방향 clamp, hover 때 요소를 옮기던 transform 과 600ms 진입 애니메이션을 없앰.
- 2026-09-16 `86f654c` — 같은 WaferInfo.ini 를 한 실행에서 한 번만 열고 존재 확인(stat) 왕복을 없앰. 단계별 시간과
  Report·INI 읽기 수를 `meta.timing` 으로 남겨 결과 화면 설정 탭에 표로 보여 줌(현장 기준선용).
- 2026-09-16 `df7622e` — 캐시에 분류 규칙 번호를 두어 규칙이 바뀌면 NAS 없이 재분류하고, INI 를 못 찾은 Report 만
  다시 읽는 '누락 복구' 옵션(CLI `--recover` · 수집 화면 체크박스)을 만듦. 장비 상태를 완료/일부 실패/접근 못 함으로
  나누고 완료 안내·홈 카드에 이름과 칩으로 보여 줌.
- 2026-09-16 `3cb481c` — 'HTML로 저장' 이 열을 골라 담아 TEST·배치·Report 가 사라지던 회귀를 고침(수집기 열·풀 그대로),
  오늘 분모 문구를 '마지막 스캔 기준' 으로(README·설정 도움말 포함), Lot 툴팁의 Wafer 장수에서 배치 시도를 분리.
  Codex 인계 프롬프트의 주장을 실데이터·Chromium 으로 재현한 결과를 '실물로 확인한 사실' 에 남김.
- 2026-09-16 `65b456e` — 현장 수집 결과 HTML 을 `dev/samples/` 에 보관(D26). NAS 없이 진짜 데이터로
  화면을 확인할 수 있다 — 지금까지 찾은 문제들이 전부 여기서 나왔다.
- 2026-09-16 `5878165` — 전주·전월 대비 삭제(D23), Lot 막대를 이름 그대로 + 60초 넘는 빈 시간마다
  나누기(D24). 4층은 추가 작업 없음(D25).
- 2026-09-16 `be5268f` — `Nothing to Scan.` 을 오류로 세기로 확정(D22). 코드는 이미 그러하므로 기록만.
- 2026-09-16 `ddad6d9` — 다시 검사(RE)는 같은 Lot 이어도 막대를 합치지 않는다(D21) — 재스캔은 실가동률을
  깎는 원인이라 합치면 손실이 보이지 않는다. 묶는 것은 REWORK 뿐.
- 2026-09-16 `acaa6ce` — **실장비 3일치(15,626행) 첫 수집 결과를 보고 고친 것들**: 오늘 분모를 마지막
  스캔까지로, TEST 는 분모에 포함·분자에서만 제외, Lot 막대 안을 조각별 색으로 + 종합 툴팁, Rework·RE 를
  REWORK 만 같은 Lot 으로 묶기(RE 는 따로 — 실가동률 손실이 보여야 한다), 오류 막대 툴팁에 발생 시각·종류·정지
  시간, `폴더 *` 가 범위 제한 중에도 동작(4층),
  AOI-10 의 빈 job 되찾기, 새 상태 표기 7종 분류.
- 2026-09-16 `1d2dc5f` — NAS 를 `read_workers` 개씩 **동시에 읽는다**(기본 8, 설정에서 조절).
  왕복 지연 2ms 를 흉내 내면 9.1초 → 2.1초. 결과·커서는 한 줄로 읽을 때와 지문까지 같다(가드).
  창 없이 돌리는 길(`python -m aoi_capacity.cli`)과 HTML 다시 쓰기 비용을 README 에 적었다.
- 2026-09-16 `845ce39` — 이 파일(`진행상황.md`)을 만들고 커밋마다 갱신하게 함 — 세션이 바뀌어도 이어서
  일할 수 있게. 가드: `test_progress_doc.py`(형식 + 코드와의 어긋남) · 옵트인 `dev/hooks/pre-commit`.
- 2026-09-16 `12032b0` — Job name(`job`·`setup`)과 Report 파일 이름을 행에 담고, 막대·표 더블클릭으로
  BatchReport 열기, 오류+정지를 막대 하나로. 열이 늘었는데도 HTML 은 689KB→402KB(문자열 풀).
- 2026-09-16 `2907285` — 수집 범위 30대로 확대, `TEST` Lot 가동률 제외. **4F 5대가 여태 수집되지 않은
  원인**(기본 목록의 `*` 자동 탐색 행)을 찾아 고침. 설정에 '수집 기간' 추가.
- 2026-09-16 `5a9946a` — 30대 전수 샘플 반영: 오류로 묻히던 표기 4종(WAFER_LOST·HW_ERROR·RECIPE_ERROR),
  자리표시 Lot, 빈 Lot 의 INI 경로, 홈 정렬을 바로잡음.
- 2026-09-16 `10d1c1e` — 샘플 도구에 전 장비 훑기(`--all`) 추가, 30대를 zip 한 장으로.
- 2026-09-16 `6b753d3` — Lot 이름의 RE 는 노랑, REWORK 는 보라(D02·D08).
- 2026-09-16 `18d1cc7` — AOI-1·8 샘플 반영: 장비마다 다른 시각 표기, Job/Setup 없는 옛 형식.
- 2026-09-16 `755c24b` — 수집 범위를 현장 테스트 4대로.
- 2026-09-16 `51e1e9f` — 실패한 배치·덮어쓴 INI·다시 검사를 실물 근거대로 계산.
- 2026-09-16 `8b25771` — 상태 표기 전수 대응, 같은 Wafer 시간 이중 계산 차단.
- 2026-09-16 `2558c48` — 코드 받기 도구(`scripts/update_code.py`).
- 2026-09-16 `13e3181` — Scanresult 경로를 `Job/Setup` 에서 얻고 `Reports` 폴더명·정렬 오류 처리.
- 2026-09-16 `3a7ae34`·`6dcf244` — 현장 샘플 수집 도구.
- 2026-09-16 `cb10b39` — 수집기와 결과 화면 분리(PyQt6 수집 전용 + 정적 HTML 한 장).
- 2026-09-16 `cbeacd4` — 수집 범위를 AOI-25 한 대로 격리, 장비 표시명·정렬 정리.
- 2026-09-13 `1d65fde` … `fdab695` — M1~M5: 수집 코어 패키지화, PyQt6 GUI, exe-lite 빌드, 자동 업데이터, 문서.
