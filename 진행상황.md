# 진행상황

> **이 파일은 커밋마다 고친다.** 세션이 바뀌어도 "지금 어디까지 왔고, 무엇이 확정이고, 무엇이 아직 확인 전인지"
> 를 여기 하나만 읽고 알 수 있어야 한다. 규칙·설계는 `CLAUDE.md`, 쓰는 법은 `README.md` 에 있다 —
> 여기에는 **상태**만 적는다(규칙을 베껴 두면 따로 낡는다).
>
> 새 세션이 시작되면: ① 이 파일 → ② `CLAUDE.md` → ③ `git log --oneline -10` 순으로 읽는다.

마지막 갱신: 2026-09-16 · 브랜치 `claude/new-session-wbided`

---

## 한 줄 요약

Camtek AOI 30대의 BatchReport/WaferInfo.ini 를 읽어 가동률을 내는 **PyQt6 수집기**와 **더블클릭으로 여는
정적 HTML 대시보드 한 장**. 코드는 30대 전수 샘플(Report 55,717개)로 검증했고, **실장비(NAS 드라이브)
검증은 아직 못 했다** — 개발 환경에서 `X:`·`M:`·`V:`·`P:`·`Y:`·`I:` 에 닿지 않는다.

## 지금 상태

| 영역 | 상태 |
|---|---|
| 수집 코어(`collect.py`) | 30대 형식 전부 대응 확인 — 읽기 실패 0 · 시각 못 읽은 Report 0. 캐시에 분류 규칙 번호(`PARSER_VERSION`)를 두어 규칙이 바뀌면 NAS 없이 재분류, '시간 미확인 Report 다시 읽기'(누락 복구) 옵션 |
| 수집 범위(`scope.py`) | **30대 전부**(AOI-1~25 + 4F-AOI-01~05). `폴더 *` 행도 제한 중에 동작 |
| 결과 화면(`template.html`) | 홈·추이·장비 비교·설정 + 일별 타임라인(포인터 층·드래그 확대·클릭 상세)/Lot·오류 표. 좁은 창 상단 탭. 바깥 요청 0건 |
| PyQt6 앱 | 수집 전용(수집 · 장비 목록 · 설정). 결과 화면은 앱 안에 없다 |
| 배포 | exe-lite + GitHub SHA 비교 자동 업데이트 (구조만 완성, 현장 배포 전) |
| 테스트 | `QT_QPA_PLATFORM=offscreen python -m pytest` — **326개 통과**(PyQt6 없는 환경 기준, ui 2개 skip) |
| **실장비 검증** | **미완료** — 아래 '다음 할 일' 참조 |

## 확정된 결정 (사용자 답변 — 추측하지 말 것)

| # | 주제 | 결정 |
|---|---|---|
| D01 | 재사용 범위 | 수집 코어만 재사용, 수집 GUI 는 새로 제작 |
| D02 | 다시 검사(RE) | 노란 표시만, 가동률에 **포함** |
| D03 | 화면 설정 | 같은 PC·브라우저에서 유지(localStorage) |
| D04 | 표시명 | `4F-AOI-01`~`05`, 범위 밖은 "수집 안 함" |
| D05 | 실패한 배치 | 오류 시간으로 세고 분자에서 제외(빨강) |
| D06 | LoadPort/Slot 행 | 배치 **1건**으로 묶는다 |
| D07 | 같은 Wafer 중복 | 시간은 한 번만, 노란 '다시 검사' 표시 |
| D08 | REWORK | RESCAN 과 **따로** 표시(보라) |
| D09 | 검사 종류 토큰 | `SRD`·`DIA`·`3D`·`EDGE`·`BUMP`·`PIDS3/5/7/9`·`RDL2/3/4`·`TPST6`·`TPDV`·`WBG`·`STRIP`·`DUMMY`·`RW` 전부 **정상** |
| D10 | `TEST` Lot | 가동률에서 **뺀다**(분자·분모·오류 건수 모두). 화면에는 회색으로 남긴다 |
| D11 | 수집 범위 | 4대 테스트 후 **30대 전부**로 확대 |
| D12 | 4층 5대 | 양산 가동 전이지만 **구별 없이 대시보드에 포함** |
| D13 | 자동 수집 | 없음 — 수동 실행만 |
| D14 | Job name | 행에 **함께 담는다**(나중에 쓸 일이 있음) |
| D15 | Report 열기 | 막대·표를 **더블클릭**하면 그 BatchReport 를 새 탭으로 |
| D16 | 오류 막대 | 오류 발생 + 그 뒤 정지를 **막대 하나**로 (툴팁에서 시각·종류·정지 시간을 나눠 보여 줌) |
| D17 | 가동률 분모 | **무조건 하루 24시간**. 단 오늘 조회는 `00:00 ~ 그날 마지막 스캔` 까지 |
| D18 | `TEST` 의 자리 | **분모에는 들어가고 분자(실가동)에서만 뺀다** — 양산을 위해 돈 게 아니라서 |
| D19 | Lot 막대 | 하나로 두되 **안을 조각별 색으로** 칠하고 마우스를 올리면 종합 정보 |
| D20 | Rework 묶기 | **REWORK 만 같은 Lot 으로 한 막대**. 구분자 차이(`TTP DIA`·`TTP-DIA`)도 같은 Lot |
| D21 | 다시 검사(RE) 묶기 | **합치지 않는다** — 같은 Lot 을 두 번 돌린 것이라 실가동률을 깎는 원인이고, 합치면 손실이 안 보인다 |
| D22 | `Nothing to Scan.` | **오류로 센다**(3일치 7건). 설비가 돌 수 있었는데 못 돈 시간이다 |
| D23 | 기간 비교 | 추이·장비 비교에서 **전주·전월·전일 대비를 전부 뺀다**. 선택한 기간 값만 |
| D24 | Lot 막대 나누기 | **Lot 이름 그대로** 묶는다(RE·REWORK·구분자 차이 전부 따로). 같은 Lot 이어도 **60초 넘게 비면 나눈다** |
| D25 | 4층 표기 | 재수집하면 일반 카드로 나오는 것으로 충분 — 층 배지·필터는 만들지 않는다 |
| D26 | 현장 데이터 보관 | 수집 결과 HTML 을 `dev/samples/` 에 **그대로 커밋한다**. 저장소가 공개인 것을 알고 있음(사용자 확정) |
| D27 | Codex 인계 제안의 구현 범위 | **정확성 우선 · 수집 가속 · 조사 화면 세 묶음 전부 + 확대 타임라인(B09)** 을 구현한다 |
| D28 | 수집 환경 | **한 PC 에서 수동 수집하고 HTML 만 공유**한다 → 중앙 수집·NAS 인접 호스트·변경 목록·자동 조절(B01·B02·B03·B12)은 보류 |
| D29 | 결과 화면 기기 | 회사 PC 큰 모니터 + **노트북 작은 창도 자주 쓴다** → 820px 미만 상단 4탭, 1280px 에서도 실측. 휴대폰은 범위 밖 |

## 실물로 확인한 사실 (근거 있는 것만)

- **Report 폴더 이름이 장비마다 다르다** — `Report`(AOI-4~16) / `Reports`(나머지 17대).
- **Scanresult 경로의 출처는 Report 안의 `Job/Setup`** 이다. 파일명 규칙은 실장비 516개 중 6개만 맞았다.
  `Job/Setup` 이 없는 옛 형식 5대(AOI-1·2·6·10·11, 값이 `/` 로 비어 있음)만 파일명 규칙으로 되돌아간다.
- **`WaferInfo.ini` 는 다시 검사하면 같은 경로에 덮어써진다**(AOI-25 9/14 `00NSP049XYG7`).
  → 첫 시도의 시각은 **복구 불가능**하다. 배치 구간 밖 INI 는 `STALE` 로 두고 시간을 쓰지 않는다.
- **실패한 배치는 Scanresult 에 흔적이 없다** — 아는 시간은 Report 의 `Batch Start~End` 뿐이다.
- **시각 표기가 두 가지다** — `15-Sep-26 05:31:04 PM`(대부분) / `9/16/2026 1:54:03 PM`(AOI-1·2).
  30대 전수에서 못 읽은 시각 **0건**.
- **`LoadPort A`·`Slot n` 은 Lot·Wafer 가 아니라 자리표시**다. Lot 이 비면 INI 경로에서 칸이 사라져
  **다른 Lot 의 INI** 를 가리킨다.
- **반송 실패 문구가 `… Batch Aborted. Skipped.` 로 끝난다** — 규칙 순서를 잘못 두면 오류가
  '건너뜀(정상)' 으로 묻힌다.
- 30대 전수 샘플 통계: Report 55,717개 · 상태 문구 34종 · Job 96종 · Lot 217종.
- 장비 위치: **AOI-1~25 는 2층, 4F-AOI-01~05 는 4층**(사용자).
- 같은 Lot 안 연속 Wafer 사이 빈 시간(3일치 11,529건): 50% 21초 · 95% 52초 · 99% 1,068초.
  Wafer 교체는 20~30초고, 그보다 길게 비는 것은 설비가 실제로 쉰 것이다 → 막대 나누는 기준 60초.
- **실장비 첫 수집(9/13~9/16, 25대 15,626행)에서 드러난 것**
  - `폴더 *` 행 때문에 **4층 5대가 한 대도 수집되지 않았다** → `*` 가 제한 중에도 도는 길을 만들었다.
  - **AOI-10 의 낮은 가동률은 설비가 쉰 게 아니라 데이터 문제였다.** Setup 이 `SETUP`(4자리 아님)이라
    파일명 규칙이 빗나가 job 이 비었고 INI 경로가 통째로 어긋났다 — 9/15 하루에만 8.8시간이 '미가동'.
  - 미분류(OTHER) 표기 7종: Clean Reference Error.(72) · Nothing to Scan.(7) · Manual Alignment Failed.(6) ·
    Scanning Multi recipe error.(3) · Prealigner failure(2) · Wafer Handling Failure(1) · Failed on MoveToStation(1).
  - INI 를 못 찾은 행은 전체의 2.1%(329행). 그중 절반이 위의 빈 job 탓이었다.
  - 첫 수집에 **28.6분**(동시 읽기 적용 전 빌드).
- **결과 화면 실측(1280·1440, Chromium)**: 막대 클릭·Enter → 상세 패널 고정(원문·복사·INI 확인 상태·Report 목록), ESC 로 닫힘,
  오류 표의 'Report 열기' 버튼이 행 클릭과 겹치지 않음, 390px 폭에서 상단 4탭이 뜨고 추이로 이동됨, 타임라인은 900px 로 유지되어
  가로 스크롤(막대 높이 25.5px). 휴대폰 폭에서는 상단 바가 아직 767px 로 넓다(범위 밖 — D29).
- **확대 타임라인 실측(1440, AOI-8 9/15 02:00~03:00 드래그)**: 막대 폭 중앙값 7px → 96px, 가동률 값은 그대로(54.341%).
  6초짜리 배치 오류는 1시간 구간에서도 2단위 최소 폭(1.9px)이라 포인터 층·Tab 으로 잡는다. 날짜·장비를 바꾸면 확대는 풀린다.
- **결과 화면의 'HTML로 저장' 이 열을 골라 담아 데이터가 깨졌었다**(실데이터로 재현): 17열 중 11열만 저장해
  `kind`·`scan_type`·`report` 가 사라졌고, 그 파일을 다시 열면 TEST 280건이 실가동에 섞여 검사시간 합이 150,204초
  늘고(9/16 평균 56.4%→59.2%) 배치 표시·Report 열기가 없어졌다. 지금은 수집기가 준 열·문자열 풀을 그대로 다시 접는다 —
  저장→재열기 전후 가동률·오류·TEST·배치 수치가 같음을 Chromium(Playwright)으로 확인.
- **같은 파일도 다음 날 열면 오늘(9/16) 평균이 56.37%→43.99% 로 바뀐다.** D17(오늘만 마지막 스캔까지)의 정상 결과다 —
  분모가 24시간으로 돌아가기 때문. 문구가 '현재까지' 라 오해를 불렀으므로 '마지막 스캔 기준' 으로 고쳤다.
- **브라우저 실측(현재 template 으로 샘플을 다시 그린 사본, Chromium)**: 타임라인 막대의 반응 영역 = 그려진 영역(여유 0px).
  1440px 창에서 축척 0.95 → 최소 막대 1.9px, 1280px 1.63px. AOI-8 9/15 막대 34개 중 8px 미만 25개. 오류 막대가 Lot 위에
  그려져 2px Lot 중앙을 가리켜도 오류 툴팁이 뜬다(3건). 툴팁 5~7줄·중앙값 178자·최대 281자(마지막 줄이 파일명 전체).
  820px 미만에서는 사이드바가 사라지고 대체 메뉴가 없다. 3일치 파일 열기 ~320ms · 날짜 이동 14ms — 화면 성능은 병목이 아니다.
  → 타임라인 맨 위에 투명 포인터 층을 두어 가장 가까운 막대(≤6px)를 잡는다. 다시 잰 결과(같은 두 날): 막대 중앙에서
  자기 자신 33/34, 오류 막대와 그 Lot 이 겹친 곳은 오류가 먼저 뜨고 Tab 으로 Lot 로 넘어간다. 툴팁은 3줄+안내 한 줄,
  중앙값 88자·최대 135자(파일명 없음), 화면 밖 0건. 막대 폭·시간 길이는 그대로다.
- **INI 읽기는 행마다 `isfile`+`open` 2회 왕복이었고 실행 안 메모가 없었다** — 3일치 후보 13,920건 중 같은 경로 반복 1,090건.
  → 이제 존재 확인 없이 바로 열고(없으면 open 이 알려 준다), 한 실행 안에서 같은 경로는 한 번만 연다(`_IniMemo`).
  단계별 시간·읽은 수·INI 요청/실제 읽기 수를 `meta.timing` 에 담아 결과 화면 설정 탭 '수집 시간' 표에 보여 준다 —
  **실장비에서 아직 재지 않았다**(다음 수집 때 이 표가 첫 기준선).
  캐시는 Report mtime 만 보므로 파서를 고쳐도(acaa6ce) 옛 행(AOI-10 NOT_FOUND 153행, 상태 재분류 93건)은 재수집해도 안 바뀌었다
  → 이제 캐시를 읽을 때 규칙 번호가 다르면 상태·검사 종류를 다시 계산하고(NAS 접근 0), INI 를 못 찾은 Report 는
  '시간 미확인 Report 다시 읽기' 를 켜면 mtime 과 상관없이 다시 읽는다(STALE·자리표시·실패 배치는 대상 아님 — 되돌아오지 않는다).
- 수집기의 장비 '완료(초록)' 는 **읽기가 끝났다**는 뜻이었지 성공이 아니었다(Report 가 전부 깨져도 초록). 이제
  '일부 실패'(주황)를 나누고, 완료 안내에 접근 못 한 장비·일부 실패 장비 이름을 적으며, 결과 화면 홈 카드에
  `수집 실패`·`일부 누락` 칩이 붙는다(`meta.devices[].status`).
- **HTML 은 수집할 때마다 통째로 다시 쓴다**(데이터가 그 안에 박혀 있다). 실측: 3일치 1.0MB·0.15초 /
  30일치 9.8MB·0.60초 / 90일치 29.3MB·1.72초, 브라우저에서 열리는 데 2.1초(90일치). 다시 쓰는 비용은
  NAS 읽기에 비하면 반올림 오차다. `file://` 페이지는 `fetch` 가 막혀(크롬에서 확인) 데이터 파일을
  따로 두는 구조는 로컬 서버 없이는 불가능하다 — `<script src>` 로 쪼개는 길만 남아 있다(아직 안 함).

## 손에 있는 현장 데이터

`dev/samples/AOI_capacity_2026-09-16_3일치.html` — 첫 현장 수집(9/13~9/16 · 25대 · 15,626행).
**NAS 없이 이 파일로 화면을 다시 그려 볼 수 있다.** 꺼내는 법과 이 파일의 한계(4층 빠짐, AOI-10 가동률
낮게 나옴)는 `dev/samples/README.md` 에 적어 뒀다.

## 다음 할 일

0. **Codex 인계 프롬프트 검토 후 확정한 구현 묶음(사용자 결정: 세 묶음 전부 + 확대 타임라인)** — 순서대로 커밋한다.
   - [x] ① 저장 회귀(P04) · 문구 정합(P11) · Wafer/배치 분리(U13)
   - [x] ② 캐시 파서 버전 + 누락 복구(P05) · 장비별 수집 상태(P06)
   - [x] ③ 단계별 계측(P01) · INI 메모/사전 확인 제거(P02)
   - [x] ④ 타임라인 hit area(U05) · 툴팁 clamp·3줄(U06·U04) · 모션(U08)
   - [x] ⑤ 클릭 상세 패널(U04) · 표(U10) · 문구(U15) · 좁은 창 탭(U09 — 노트북 작은 창도 자주 씀)
   - [x] ⑥ 확대 타임라인(B09)
   - 보류(사용자: 한 PC 수동 수집·HTML 공유): 중앙 수집·NAS 인접 호스트·변경 목록·자동 조절(B01·B02·B03·B12), SQLite·Worker(P09).
1. **고친 것을 실장비에서 다시 확인** (사용자 실행 대기) — `python scripts\update_code.py` 뒤 재수집.
   - 4층 5대가 이번엔 잡히는지(`폴더 *` 행 그대로 둬도 된다).
   - **AOI-10 의 `R_TB500 TOP D-DIE_0860312PD` 배치들이 INI 를 찾는지** — 9/15 가동률이 24% → 60% 대로
     올라가야 맞다. 여전히 NOT_FOUND 면 그 Job 의 Scanresult 폴더 이름이 다른 것이므로 실물 확인이 필요하다.
   - 동시 읽기 8개로 수집 시간이 28.6분에서 얼마나 줄었는지 — 설정 탭 '수집 시간' 표(단계별 시간 · INI 요청→실제 읽기)를 적어 둘 것.
     첫 수집 · 변경 없는 재수집 · 누락 복구를 따로 잰다.
   - 더블클릭 Report 열기가 실제 NAS 경로에서도 되는지.
2. 배포(exe-lite) 현장 설치 — 아직 한 번도 안 했다.

## 확인 대기 (답이 오면 코드에 반영)

- 타임라인 막대에서 **Enter** 는 이제 상세 패널을 고정한다(클릭과 같음). 전에는 Report 를 바로 열었다 — 키보드로 Report 를 열려면
  상세 패널의 'Report 열기' 버튼을 쓴다. 이대로 둘지.

- 가동률 분모: 지금은 **하루 24시간 고정**. 실제 가동 계획 시간(비가동 계획 제외)을 쓸지.
- `4F-AOI-03·04·05` 는 Report 가 8~23개뿐이다(다른 장비는 수백~수천). 셋업용이라면 별도 표시가 필요한지.

## 작업 기록

새 항목을 **맨 위**에 넣는다. 형식: `- <날짜> <커밋> — 무엇을·왜`

지금 만드는 커밋의 해시는 아직 없으므로 `(작업 중)` 으로 적고, **다음 커밋 때 그 자리를 채우면서**
자기 항목을 `(작업 중)` 으로 새로 올린다(`git commit --amend` 로 채우면 해시가 또 바뀌어 헛돈다).

- 2026-09-16 `(작업 중)` — 타임라인 그리기를 `drawBand` 로 분리해 개요 아래에 드래그로 고른 구간(10분~6시간)을 크게 다시
  그리는 확대 패널을 둠(같은 dayMetrics 결과 — 숫자는 안 바뀐다). 1시간·4시간·◀▶·닫기.
- 2026-09-16 `e95e1c7` — 클릭·Enter 로 고정되는 상세 패널(원문 복사·INI 확인 상태·Report 목록), 표에 'Report 열기'
  버튼과 '원본 n개' 표시, 범례·설정·수집시각 문구를 짧게(한정어는 유지), 820px 미만 상단 4탭과 타임라인 가로 스크롤.
- 2026-09-16 `41e240d` — 타임라인에 포인터 층(가장 가까운 막대 ≤6px, 겹치면 좁은 쪽·같은 Lot 이면 오류 먼저,
  Tab 으로 전환), 툴팁을 3줄로 줄이고 네 방향 clamp, hover 때 요소를 옮기던 transform 과 600ms 진입 애니메이션을 없앰.
- 2026-09-16 `86f654c` — 같은 WaferInfo.ini 를 한 실행에서 한 번만 열고 존재 확인(stat) 왕복을 없앰. 단계별 시간과
  Report·INI 읽기 수를 `meta.timing` 으로 남겨 결과 화면 설정 탭에 표로 보여 줌(현장 기준선용).
- 2026-09-16 `df7622e` — 캐시에 분류 규칙 번호를 두어 규칙이 바뀌면 NAS 없이 재분류하고, INI 를 못 찾은 Report 만
  다시 읽는 '누락 복구' 옵션(CLI `--recover` · 수집 화면 체크박스)을 만듦. 장비 상태를 완료/일부 실패/접근 못 함으로
  나누고 완료 안내·홈 카드에 이름과 칩으로 보여 줌.
- 2026-09-16 `3cb481c` — 'HTML로 저장' 이 열을 골라 담아 TEST·배치·Report 가 사라지던 회귀를 고침(수집기 열·풀 그대로),
  오늘 분모 문구를 '마지막 스캔 기준' 으로(README·설정 도움말 포함), Lot 툴팁의 Wafer 장수에서 배치 시도를 분리.
  Codex 인계 프롬프트의 주장을 실데이터·Chromium 으로 재현한 결과를 '실물로 확인한 사실' 에 남김.
- 2026-09-16 `65b456e` — 현장 수집 결과 HTML 을 `dev/samples/` 에 보관(D26). NAS 없이 진짜 데이터로
  화면을 확인할 수 있다 — 지금까지 찾은 문제들이 전부 여기서 나왔다.
- 2026-09-16 `5878165` — 전주·전월 대비 삭제(D23), Lot 막대를 이름 그대로 + 60초 넘는 빈 시간마다
  나누기(D24). 4층은 추가 작업 없음(D25).
- 2026-09-16 `be5268f` — `Nothing to Scan.` 을 오류로 세기로 확정(D22). 코드는 이미 그러하므로 기록만.
- 2026-09-16 `ddad6d9` — 다시 검사(RE)는 같은 Lot 이어도 막대를 합치지 않는다(D21) — 재스캔은 실가동률을
  깎는 원인이라 합치면 손실이 보이지 않는다. 묶는 것은 REWORK 뿐.
- 2026-09-16 `acaa6ce` — **실장비 3일치(15,626행) 첫 수집 결과를 보고 고친 것들**: 오늘 분모를 마지막
  스캔까지로, TEST 는 분모에 포함·분자에서만 제외, Lot 막대 안을 조각별 색으로 + 종합 툴팁, Rework·RE 를
  REWORK 만 같은 Lot 으로 묶기(RE 는 따로 — 실가동률 손실이 보여야 한다), 오류 막대 툴팁에 발생 시각·종류·정지
  시간, `폴더 *` 가 범위 제한 중에도 동작(4층),
  AOI-10 의 빈 job 되찾기, 새 상태 표기 7종 분류.
- 2026-09-16 `1d2dc5f` — NAS 를 `read_workers` 개씩 **동시에 읽는다**(기본 8, 설정에서 조절).
  왕복 지연 2ms 를 흉내 내면 9.1초 → 2.1초. 결과·커서는 한 줄로 읽을 때와 지문까지 같다(가드).
  창 없이 돌리는 길(`python -m aoi_capacity.cli`)과 HTML 다시 쓰기 비용을 README 에 적었다.
- 2026-09-16 `845ce39` — 이 파일(`진행상황.md`)을 만들고 커밋마다 갱신하게 함 — 세션이 바뀌어도 이어서
  일할 수 있게. 가드: `test_progress_doc.py`(형식 + 코드와의 어긋남) · 옵트인 `dev/hooks/pre-commit`.
- 2026-09-16 `12032b0` — Job name(`job`·`setup`)과 Report 파일 이름을 행에 담고, 막대·표 더블클릭으로
  BatchReport 열기, 오류+정지를 막대 하나로. 열이 늘었는데도 HTML 은 689KB→402KB(문자열 풀).
- 2026-09-16 `2907285` — 수집 범위 30대로 확대, `TEST` Lot 가동률 제외. **4F 5대가 여태 수집되지 않은
  원인**(기본 목록의 `*` 자동 탐색 행)을 찾아 고침. 설정에 '수집 기간' 추가.
- 2026-09-16 `5a9946a` — 30대 전수 샘플 반영: 오류로 묻히던 표기 4종(WAFER_LOST·HW_ERROR·RECIPE_ERROR),
  자리표시 Lot, 빈 Lot 의 INI 경로, 홈 정렬을 바로잡음.
- 2026-09-16 `10d1c1e` — 샘플 도구에 전 장비 훑기(`--all`) 추가, 30대를 zip 한 장으로.
- 2026-09-16 `6b753d3` — Lot 이름의 RE 는 노랑, REWORK 는 보라(D02·D08).
- 2026-09-16 `18d1cc7` — AOI-1·8 샘플 반영: 장비마다 다른 시각 표기, Job/Setup 없는 옛 형식.
- 2026-09-16 `755c24b` — 수집 범위를 현장 테스트 4대로.
- 2026-09-16 `51e1e9f` — 실패한 배치·덮어쓴 INI·다시 검사를 실물 근거대로 계산.
- 2026-09-16 `8b25771` — 상태 표기 전수 대응, 같은 Wafer 시간 이중 계산 차단.
- 2026-09-16 `2558c48` — 코드 받기 도구(`scripts/update_code.py`).
- 2026-09-16 `13e3181` — Scanresult 경로를 `Job/Setup` 에서 얻고 `Reports` 폴더명·정렬 오류 처리.
- 2026-09-16 `3a7ae34`·`6dcf244` — 현장 샘플 수집 도구.
- 2026-09-16 `cb10b39` — 수집기와 결과 화면 분리(PyQt6 수집 전용 + 정적 HTML 한 장).
- 2026-09-16 `cbeacd4` — 수집 범위를 AOI-25 한 대로 격리, 장비 표시명·정렬 정리.
- 2026-09-13 `1d65fde` … `fdab695` — M1~M5: 수집 코어 패키지화, PyQt6 GUI, exe-lite 빌드, 자동 업데이터, 문서.
