# 진행상황

> **이 파일은 커밋마다 고친다.** 세션이 바뀌어도 "지금 어디까지 왔고, 무엇이 확정이고, 무엇이 아직 확인 전인지"
> 를 여기 하나만 읽고 알 수 있어야 한다. 규칙·설계는 `CLAUDE.md`, 쓰는 법은 `README.md` 에 있다 —
> 여기에는 **상태**만 적는다(규칙을 베껴 두면 따로 낡는다).
>
> 새 세션이 시작되면: ① 이 파일 → ② `CLAUDE.md` → ③ `git log --oneline -10` 순으로 읽는다.

마지막 갱신: 2026-09-23 · 브랜치 `main`(기본 브랜치 · 업데이트 기준) · 작업 브랜치 `claude/youthful-ptolemy-fnw4qn`(9/19~20: 백업 폴더 조회 · 디자인 handoff · D47 재설계 화면 이식 + 검수 답 D52~D55 · **개선 계획 P0~P5 전부(9/20 저녁~밤)** — 단계마다 main 에 합침(D65), 현장 업데이트 대상. P6 검토 아티팩트까지 전달 — 남은 것은 실장비·Windows 확인과 사용자 답 7개)

---

## 한 줄 요약

Camtek AOI 30대의 BatchReport/WaferInfo.ini 를 읽어 가동률을 내는 **PyQt6 수집기**와 **더블클릭으로 여는
정적 HTML 대시보드 한 장**. 코드는 30대 전수 샘플(Report 55,717개)로 검증했고, **실장비(NAS 드라이브)
검증은 아직 못 했다** — 개발 환경에서 `X:`·`M:`·`V:`·`P:`·`Y:`·`I:` 에 닿지 않는다.

## 지금 상태

| 영역 | 상태 |
|---|---|
| 수집 코어(`collect.py`) | 30대 형식 전부 대응 확인 — 읽기 실패 0 · 시각 못 읽은 Report 0. 캐시에 분류 규칙 번호(`PARSER_VERSION` 5)를 두어 규칙이 바뀌면 NAS 없이 재분류·합성 행 재구축, '시간 미확인 Report 다시 읽기'(누락 복구) 옵션. 행 **25열**(`ROW_SCHEMA_VERSION` 6 — `faults`·`scanned_dice`·`yield` + `issue_codes`). **Scanresult 백업 폴더 조회**(`devices.scan_dirs_of` · `collect.ini_roots_for`, `ini_match=MOVED_ONLY` 추가) — 실장비 전후 비교 미완. **P1(9/20 저녁)**: 수집 모드 `--refresh-window N`(이력 보존 재읽기) · `--rebuild-all`(후보 캐시 → 검증 → 교체, `--full` 은 별칭) · 캐시 저장 트랜잭션 + 손상 캐시 `.bad-<시각>` 보존 · CSV 잠김은 부분 성공(exit 3). GUI 체크 '최근 N일 다시 읽기(이력 보존)'. **P3(9/20 밤)**: 장비 안정 키 + Report 키 `dev:장비|상대경로` + 옛 캐시 멱등 이관(C03) · 바뀐 이유 있을 때만 캐시 저장(C04) · 장비 색인(C09) · HTML 순차 쓰기 181→107MB(C07) · Scanresult 대소문자 중복 제거(C05) · 장비 확인 병렬(C08) · `issue_codes`(C12) · 설정 검사 `normalize_config`(C13) · `--update` 자식 프로세스 + bat 회전(C14) · pythonw 안내(C16) · `meta.dashboard_settings`(D14). **실장비에서 새 모드·이관 실행은 아직**(옛 캐시 이관은 첫 실행에서 한 번 일어난다 — 로그의 identity 줄을 볼 것) |
| 수집 범위(`scope.py`) | **30대 전부**(AOI-1~25 + 4F-AOI-01~05). `폴더 *` 행도 제한 중에 동작 |
| 결과 화면(`template.html`) | **D47 이식 + 모델 v3 + P4 화면(9/20)** — P4: 탭 'TB500 · Kendall'(D59) · 기록 없음 분리(D06) · Lot 선택 키에 Report(D16) · 옛 추정 코드 제거(D15) · 포커스/스크롤 복원·inert·Tab 트랩(D05) · 표 가로 스크롤·이름표 접기(D10) · 문구(D11) · 품질 한 줄(D13) · `meta.dashboard_settings`(D14, **수집기가 넣는 건 P3**) · 평균 fault(D08) · 브라우저 테스트 S06. 모델은 `buildModelV3`(MODEL_VERSION 3, D56~D58·D63·D64·D04·D09; legacy 프로필은 디자인 동일성 가드 전용).  재설계 구조: 가동률(요약 카드 3 · 장비별 하루 막대 · 층/정렬) · Error(기간 · 건수/대기 시간 · 유형별 · Job별) · 추이(일/주/월 · 장비별) · 리포트(TB500·Kendall 표기명 21개, 배치 시작~종료 회귀) + 장비/Error/유형·Job 팝업, 라이트 단일. 모델은 `buildModel`(= `make_aoi_data.js` + D48 제품 유지 2건, 열 때마다 계산). 남긴 제품 기능: Report 열기 · 수집 상태 칩 · 수집 안 함 · 사본 저장. 바깥 요청 0건. 30일치 Chromium 열기 ~3.4초 · 콘솔 오류 0. 검수 답(D52~D55) 반영: PASS·원인 없는 Report 의 중단 = Error, INI 없는 Pass 행은 배치 구간으로 추정(행 단위). **main 에 합침(9/20) — 현장 화면 확인 대기** |
| PyQt6 앱 | 수집 전용(수집 · 장비 목록 · 설정). 결과 화면은 앱 안에 없다 |
| 결과 화면 **재설계 프로토타입**(`docs/design/dashboard-redesign/`) | Claude Design 9/19 handoff + 추출 스크립트(9/20). **제품에 이식됐으므로 이제 참고 자료**다 — 프로토타입 규칙과 제품의 차이는 D48 의 두 항목(대기 상한 · 오늘 분모)뿐. 오프라인 번들은 `design_bundle.py` 로 여전히 만들 수 있다 |
| 배포 | exe-lite + GitHub SHA 비교 자동 업데이트 (구조만 완성, 현장 배포 전). 기준 브랜치 **`main`**(`updater.DEFAULT_BRANCH` · `update_code.py` `BRANCH`). **P1(9/20 저녁, D61)**: main SHA 의 Actions `tests` 성공 시에만 받음(아니면 '보류' + 이유) · `archive/<SHA>.zip` · zip 항목 검사 · 허용 목록 페이로드 · 파일별 저널 롤백 · TLS 무검증 폴백 폐지 · pip 10분 상한 · 배포 zip 잔재 제외. ★ **`tests.yml` 이 main 에 처음 올라간 뒤 첫 run 이 초록이 되기 전까지는 모든 업데이트가 '보류'** 다(설계대로) — 현장 PC 는 그 다음 커밋부터 받는다 |
| 테스트 | `QT_QPA_PLATFORM=offscreen python -m pytest` — **전체 705개 중 693 통과 · 12 skip**(PyQt6·playwright·yaml 없는 인터프리터 기준; skip 분은 PyQt6 venv 에서 146개 따로 돌려 통과, 9/20 밤). **CI `tests.yml`(P1)** 은 main 의 두 run 모두 초록(core + browser 잡, 브라우저 테스트 4개 실제 Chromium 실행 확인) — 업데이터 D61 게이트가 받을 SHA 가 생겼다. P5 로 화면 계약 테스트는 제품 제약 + 실행 검사(하네스 globals/screen 모드 · 브라우저 사본 저장 왕복)로 바뀌었고, `test_progress_doc.py` 가 CLAUDE.md 숫자를 코드와 대조한다. `test_dashboard_js.py` 18개는 새 모델을 Node 로 **실제 실행**(Lot 단위 Error · Rescan 정의 · Job 그룹 · 대기/분모 D48 · 추정 근거 · lotName · 정렬), 그중 slow 1개가 **두 스위치를 끄면 `make_aoi_data.js` 출력과 장비-일 828개 전부 같음**을 본다. `test_template_contract.py` 19개(라이트 단일 · CR 규칙 3자 일치 · 용어 · 4탭 3팝업 · Report 열기 · 풀 접기). `test_status_mapping.py` 213문구 Python·JS 대조 그대로. 화면은 Chromium(Playwright)으로 따로 실측(원장) |
| 9/18 실행 계획(D36~D46) | **phase 0~8 구현·검증 완료**(브랜치 `claude/new-session-12ftwa`, 원장 `dev/samples/ledger_2026-09-18.md`). phase 9(P1)는 다음 세션 |
| **실장비 검증** | **미완료** — 아래 '다음 할 일' 참조. 새 규칙(D36~D46)으로 재수집한 결과를 사용자가 화면에서 확인하는 것도 대기 |

## 확정된 결정 (사용자 답변 — 추측하지 말 것)

| # | 주제 | 결정 |
|---|---|---|
| D01 | 재사용 범위 | 수집 코어만 재사용, 수집 GUI 는 새로 제작 |
| D02 | 다시 검사(RE) | 노란 표시만, 가동률에 **포함** |
| D03 | 화면 설정 | 같은 PC·브라우저에서 유지(localStorage) |
| D04 | 표시명 | `4F-AOI-01`~`05`, 범위 밖은 "수집 안 함" |
| D05 | 실패한 배치 | 오류 시간으로 세고 분자에서 제외(빨강) |
| D06 | LoadPort/Slot 행 | 배치 **1건**으로 묶는다 |
| D07 | 같은 Wafer 중복 | 시간은 한 번만, 노란 '다시 검사' 표시 |
| D08 | REWORK | RESCAN 과 **따로** 표시(보라) |
| D09 | 검사 종류 토큰 | `SRD`·`DIA`·`3D`·`EDGE`·`BUMP`·`PIDS3/5/7/9`·`RDL2/3/4`·`TPST6`·`TPDV`·`WBG`·`STRIP`·`DUMMY`·`RW` 전부 **정상** |
| D10 | `TEST` Lot | 가동률에서 **뺀다**(분자·분모·오류 건수 모두). 화면에는 회색으로 남긴다 |
| D11 | 수집 범위 | 4대 테스트 후 **30대 전부**로 확대 |
| D12 | 4층 5대 | 양산 가동 전이지만 **구별 없이 대시보드에 포함** |
| D13 | 자동 수집 | 없음 — 수동 실행만 |
| D14 | Job name | 행에 **함께 담는다**(나중에 쓸 일이 있음) |
| D15 | Report 열기 | 막대·표를 **더블클릭**하면 그 BatchReport 를 새 탭으로 |
| D16 | 오류 막대 | 오류 발생 + 그 뒤 정지를 **막대 하나**로 (툴팁에서 시각·종류·정지 시간을 나눠 보여 줌) |
| D17 | 가동률 분모 | **무조건 하루 24시간**. 단 오늘 조회는 `00:00 ~ 그날 마지막 스캔` 까지 |
| D18 | `TEST` 의 자리 | **분모에는 들어가고 분자(실가동)에서만 뺀다** — 양산을 위해 돈 게 아니라서 |
| D19 | Lot 막대 | 하나로 두되 **안을 조각별 색으로** 칠하고 마우스를 올리면 종합 정보 |
| D20 | Rework 묶기 | **REWORK 만 같은 Lot 으로 한 막대**. 구분자 차이(`TTP DIA`·`TTP-DIA`)도 같은 Lot |
| D21 | 다시 검사(RE) 묶기 | **합치지 않는다** — 같은 Lot 을 두 번 돌린 것이라 실가동률을 깎는 원인이고, 합치면 손실이 안 보인다 |
| D22 | `Nothing to Scan.` | **오류로 센다**(3일치 7건). 설비가 돌 수 있었는데 못 돈 시간이다 |
| D23 | 기간 비교 | 추이·장비 비교에서 **전주·전월·전일 대비를 전부 뺀다**. 선택한 기간 값만 |
| D24 | Lot 막대 나누기 | **Lot 이름 그대로** 묶는다(RE·REWORK·구분자 차이 전부 따로). 같은 Lot 이어도 **60초 넘게 비면 나눈다** |
| D25 | 4층 표기 | 재수집하면 일반 카드로 나오는 것으로 충분 — 층 배지·필터는 만들지 않는다 |
| D26 | 현장 데이터 보관 | 수집 결과 HTML 을 `dev/samples/` 에 **그대로 커밋한다**. 저장소가 공개인 것을 알고 있음(사용자 확정) |
| D27 | Codex 인계 제안의 구현 범위 | **정확성 우선 · 수집 가속 · 조사 화면 세 묶음 전부 + 확대 타임라인(B09)** 을 구현한다 |
| D28 | 수집 환경 | **한 PC 에서 수동 수집하고 HTML 만 공유**한다 → 중앙 수집·NAS 인접 호스트·변경 목록·자동 조절(B01·B02·B03·B12)은 보류 |
| D29 | 결과 화면 기기 | 회사 PC 큰 모니터 + **노트북 작은 창도 자주 쓴다** → 820px 미만 상단 4탭, 1280px 에서도 실측. 휴대폰은 범위 밖 |
| D30 | 화면 용어·색 | **검사→가동, 재검사→재스캔, 재작업→Rework, 오류→Error, 시험→Test**(Test 색은 미가동 회색과 다른 분홍 점무늬). 중복스캔(청록 사선)을 새 범례로. 원본 상태 문구·Lot 이름·파일명은 그대로 |
| D31 | 중복스캔 | **같은 자재를 이어서 여러 번 실제로 스캔하면 정상 가동으로 인정해 가동시간에 넣고**, 그 반복 시도의 막대는 '중복스캔' 으로 구분한다. ★ D07 의 '같은 시각 중복 = 재검사(노랑)' 는 **바뀐다** — 같은 시각 참조는 데이터 중복(시간 1번·이력 보존)이지 반복이 아니며, 노랗게 칠하지 않는다 |
| D32 | 다른 장비 재스캔 | 8호기에서 스캔했거나 Error 난 **같은 자재**를 9호기 등 다른 장비에서 다시 스캔하면 **재스캔**. 8호기의 시간은 8호기에, 9호기의 시간은 9호기에 각각 남는다(옮기지 않음) |
| D33 | 가동률 저하 사유 | 홈에 **사유별(Error 구간 · Error 후 정지(추정) · Test · 미가동·사유 미확인) 가로 막대 차트**. 클릭 → 사유 상세 → 장비별 기여 → 이벤트 → 장비 상세로 이동, '사유 분석으로' 복귀. 반복 가동은 저하 사유가 아니라 별도 카드 |
| D34 | 장비 상세 위치 | 장비를 클릭하면 상세가 목록 맨 아래가 아니라 **클릭한 장비 버튼 바로 아래**에 열린다(카드는 그 줄 전체로, 표는 바로 다음 행). 같은 장비 재클릭으로 닫고, 동시에 하나만 |
| D35 | 중단(Abort) | **→ D41 로 개정**(원문은 그대로 둔다). `Aborted.`·`Wafer aborted by user.` 는 **Error 가 아니다** — 사람이 세운 것이다. Error 건수·빨간 막대에서 빼고 그 뒤 공백도 '정지(추정)' 로 돌리지 않는다. 대신 '중단' 으로 따로 보여 주고 **분모에는 넣되 실가동(분자)에서는 뺀다**(스캔이 끝나지 않았다). 반송 실패(`… Batch Aborted. Skipped.`)는 그대로 Error |
| D36 | 공정 동일성 | **Job 원문이 정확히 같을 때만 같은 공정**. 같은 자재의 이력에서 그 Job 의 앞선 시도를 찾아 반복 관계를 판정한다(중간에 다른 Job 이 있어도 끊지 않는다). 그 Job 의 앞선 기록이 없으면 확인된 첫 기록. 빈 Job 은 같은 공정의 근거가 못 된다. Job 이 달라도 자재 이력에는 시각순으로 전부 남고 시간은 각 장비에 그대로다(D32). 예외 한 건: **YCG-EDGE / GWM5K09-F4 (8/25) 는 AOI-11 → AOI-9 재스캔으로 확정**(사용자 확인 — Job `2D@R2-…`/`2D@R3-…` 이 달라도, 겹침 135초는 설비 시계 오차로 추정. 시각을 옮기거나 보정값을 만들지 않는다. 나머지 보류 3건에는 적용하지 않는다) |
| D37 | 같은 INI 시각을 두 Report 가 참조 | INI 는 재검사로 덮어써지므로 앞 시도가 Error 고 곧 다시 돌려 Pass 하면 두 Report 가 같은 시각을 갖는다. **그 시각을 자기 Batch 구간에 엄격히 포함하는 Report 가 하나뿐이면 그 Report 가 시간을 갖는다**(뒤의 정상 스캔). ±10분 여유로만 걸린 나머지는 '시간 미확인 · INI 가 나중 검사로 덮어써짐' — 가동/Error 시간에 넣지 않되 **Error 건수는 남기고** 발생 시각은 그 Report 의 Batch 구간으로 표시. `BATCH_WINDOW_MARGIN_SEC`(600) 값은 그대로 |
| D38 | 부분 실패 배치의 Slot Error | 일부 Wafer 는 정상 스캔됐지만 LoadPort/Slot 자리표시 행에 명시적 Error(ID 읽기 실패·반송 실패·정렬 오류 등)가 남은 Report 는 **Report 당 Error 사건 1건 + 영향 Slot 수**. 행마다 세지 않는다. 정확 발생 시각 없음(시간 미확인 · Batch 범위만 '범위 추정' 배지). Batch 전체 시간을 Error 시간으로 복사하지 않는다. 통째로 실패한 배치(D06 배치 1건)가 있는 Report 에는 만들지 않는다 |
| D39 | 자재 키 | **사이에 뭐가 있든 이름이 같으면 같은 자재 · 대소문자 무시**: Lot 은 영숫자 외 글자를 구분자로 나눠 RE/RESCAN/REWORK/TEST 토큰만 뺀 뒤 이어 붙여 대문자로, Wafer ID 는 구분자·대소문자만 정규화(앞자리 0 · 숫자 접미사는 그대로). 장비명은 넣지 않는다. **화면의 Lot 표시·Lot 막대 묶기(D24)·검색·CSV 는 원문 그대로** — 정규화는 자재 이력 연결에만 쓴다 |
| D40 | 보관 화면의 기준시각 | 화면의 '오늘' 은 브라우저의 오늘이 아니라 **수집 시각(`meta.generated_iso`)** 이다. D17(오늘만 00:00~마지막 스캔)은 그대로고 '오늘' 의 정의만 바뀐다 → 며칠 뒤 열어도 같은 숫자. 상단에 '9/18 12:59 수집 기준' 을 항상 표시. `generated_iso` 가 없는 옛 파일·데모만 브라우저의 오늘 |
| D41 | 중단(Abort) 재정의(D35 개정) | **같은 Report 에 정상 스캔(PASS)이 하나도 없으면** 원인 없는 `Aborted.`·`Wafer aborted by user.` 를 **Error 와 똑같이** 센다(Error 건수·빨간 막대·그 뒤 정지(추정)). PASS 가 하나라도 있으면 그 단순 중단은 **가동(분자)에 넣는다**. 기술 원인이 있으면 PASS 유무와 무관하게 Error. `Cancelled`·`-` 는 기본 Error 에서 뺀다. 둘 다 '중단' 이라는 이름은 남긴다(Error 중단 = 빨강+교차 빗금, 가동 중단 = 회청색 교차 빗금). 반송 실패는 그대로 Error |
| D42 | Rework + 재스캔 | 같은 Job · 같은 Wafer ID 가 다시 스캔되면 **대표 색은 재스캔(노랑) + Rework 배지**, 툴팁·표에 'Rework · 재스캔' 둘 다. Wafer ID 가 다르면 각각 Rework 로만(Q03 해소) |
| D43 | 원인과 결과를 다른 축으로 | 상태 문구에서 **원인(cause)** 과 **종료 결과(outcome)** 를 따로 뽑는다. 원인 규칙을 전부 먼저 보고, 남은 것에서 결과(PASS·SKIPPED·USER_ABORT·USER_CANCELLED·ABORTED·FAILED·UNKNOWN)를 정한다. `norm_status` 는 '원인이 있으면 원인, 없으면 결과'. `Failed to read wafer id … Wafer Skipped.` 는 원인 ID_READ_ERROR + 결과 SKIPPED(전에는 건너뜀에 묻혔다). `Cancelled` = 사용자 취소, `-` = 결과 미확인 — 둘 다 기본 Error 에서 제외하고 보존. 원문은 절대 바꾸지 않는다 |
| D44 | 복구 기록 없는 마지막 Error 의 정지(추정) | **관측 종료(로드한 데이터의 마지막 시각)까지만** 센다. 다음 날로 늘리지 않고 '정지 확정' 으로 표현하지 않는다(이미 구현 — 확인 대기에서 옮김) |
| D45 | 타임라인 Enter | **Enter = 상세 패널 고정**(클릭과 같음). Report 는 상세의 'Report 열기' 버튼으로 연다(이미 구현 — 확인 대기에서 옮김) |
| D46 | 가동률 분모 | **하루 24시간 고정 그대로**(D17). '실제 가동 계획 시간' 은 쓰지 않는다. 30일치 첨부는 `dev/samples/` 에 gzip 으로 보관(D26 의 연장) |
| D47 | 재설계 화면 이식 방식 (9/20) | **규칙을 먼저 확정하고 제품 `template.html` 을 재설계 구조(가동률 · Error · 추이 · 리포트 4탭 + 장비/Error/유형/Job 팝업, 라이트 단일)로 교체**한다. 착수는 디자인 세션의 추출 스크립트를 받은 뒤(DQ4-A) |
| D48 | 이식 규칙 — 디자인 채택 7 · 제품 유지 2 (9/20) | **디자인 채택**: ① 시각 확인 비율 50% 미만인 장비-일은 Report 배치 시작~종료로 **추정**(메인은 같은 모양, 팝업에서만 '(추정)' — 절대 규칙 5 개정 필요) ② Error 건수 **Lot 단위**(같은 Lot 같은 유형 1건) ③ 재스캔 정의 — 앞선 시도 Pass → Rescan(장비 무관), 앞선 Error → 정상 Scan(중복스캔 구분 폐지, D30~D32 개정) ④ Job 병합(통계만, 표기 원문, 표기명 21개, R접두어 분리 — D36 개정) ⑤ Lot 이름은 Report 파일명에서 추출·꼬리표 정리(lotKey 규칙 개정) ⑥ 라이트 단일 테마(다크 폐기, `ui/theme.py` 연동은 별도) ⑦ 용어 Scan · Rescan · Test · Error · 에러 후 대기 · 대기(D30 개정). **제품 유지**: Error 후 대기 상한 = 관측 종료까지(D44, 240분 cap 안 씀) · 하루 분모 = 오늘만 00:00~마지막 스캔(D17·D46) |
| D49 | 리포트 배치시간 (9/20) | **Report 의 배치 시작~종료**로 회귀(첫~마지막 Wafer 아님). 표본 5개 미만 Job 은 회귀를 내지 않는다 |
| D50 | 4층 5대 INI (9/20) | **자동 주기 수집은 도입하지 않는다**(규칙 3 유지). 4층은 D48-① 추정으로 본다 |
| D51 | Error 팝업 해석 (9/20) | 유형/Job 팝업에서 넘어오면 그 유형·Job 필터 칩(해제 가능) · 기간 모드에서는 상단 날짜로 열고 팝업 안에서 날짜 이동 — **그대로** |
| D52 | 원인 없는 중단 (9/20 검수 답) | **Report 에 PASS 도 원인 Error 도 없이 중단(`Aborted…`)만 남았으면(뒤에 Skipped 가 따라옴) 그 중단이 Error** 다(유형 ABORTED, 그 뒤 대기 포함). 중단 앞에 다른 내용이 있으면 **그 내용으로 마무리**한다 — PASS 면 정상, 원인 Error 면 그 원인(D41 의 취지 복원, `RULES.abortIsError`) |
| D53 | Test 판정 (9/20 검수 답) | **Lot 이름에 `TEST` 가 들어가면 전부 Test** 다(부분 문자열, 디자인 규칙 그대로). 수집기 `scan_type` 의 낱말 규칙과 2행 차이는 화면이 우선 |
| D54 | 배치 시각 추정 범위 (9/20 검수 답, D48-① 개정) | **50% 문턱이 아니라 INI 가 없는 행은 모두 추정** — Report 에 있는 내용(배치 시작~종료)으로만. Pass 행에만 적용하고 Skipped·중단·미확인 행은 추정하지 않는다(`RULES.estimateFromBatch`) |
| D55 | main 합치기 (9/20 검수 답) | **지금 바로 합친다**(확인 1~3 답 반영 뒤). 확인 4(평균 감각)는 미정으로 두고 현장에서 본다 |
| D56 | 시간 배분 규칙 (9/20 개선 계획 확인 1) | 한 장비의 1분은 한 번만 센다. ① **측정 우선** — INI 시각 구간을 먼저 놓고 겹치면 Error > Scan > Rescan > Test 순으로 배타 배정 ② **배치 창 나눠 갖기** — Report 의 배치 시작~종료에서 측정 구간을 뺀 나머지를 그 Report 의 INI 없는 행들이 **똑같이** 나눠 갖는다(Pass 행은 이력대로 Scan/Rescan, Error 행은 Error, Skipped·중단 행은 0 — PASS·원인 없는 Report 는 D52) ③ **Error 후 대기 = Error 를 담은 Report 가 끝난 뒤** 다음 활동까지(없으면 관측 종료·자정 중 이른 것까지, D44). Report 안에서 Error 뒤에 스캔이 이어지면 그 사이는 스캔. 계획의 '미분류 생산' 은 쓰지 않는다. D54(행마다 배치 창 전체)는 ②로 개정. 30일치 예상: 평균 59.56 → 54.09%, 대기 449 → 845h |
| D57 | 수집한 날의 분모 (9/20 확인 2) | **수집 당일도 모든 장비의 마지막 기록(관측 종료)** 까지가 분모다(장비별 마지막 기록 아님). 기록이 없는 장비는 0% 로 채우지 않고 평균에서 빼되 대수를 표시. D17·D46 의 '그날 마지막 기록' 을 모든 장비 기준으로 확정. 9/18 평균 66.52 → 61.31% |
| D58 | 시각 없는 Error 의 시간 (9/20 확인 8) | INI 가 없는 Error 행은 3분 표식이 아니라 **그 Report 의 배치 창에서 측정 구간을 뺀 빈 시간**을 Error 로 받는다(INI 없는 Pass 행과 함께면 D56-② 대로 똑같이 나눔). 배치가 통째로 실패해 INI 가 없으면 배치 전체(D52 그대로) |
| D59 | 리포트 탭 (9/20 확인 5) | **TB500·Kendall 전용 탭으로 두고 이름을 'TB500 · Kendall' 로** 바꾼다. 범위는 표기명 21개 Job 그대로, 탭 안에 '표기명 21개 Job 만(Lot 의 27%)' 안내 한 줄. 전체 Job 보기는 만들지 않는다 |
| D60 | 재수집 모드 (9/20 확인 3) | `--refresh-window N`(창 안의 캐시된 Report 도 다시 읽고 창 밖·실패 항목의 이전 결과 보존)과 `--rebuild-all`(보관 기간 전부를 새 후보 캐시에 모아 검증 뒤 교체)을 새로 만들고, **`--full` 은 `--rebuild-all` 의 별칭**(이력 삭제 없음)으로 바꾼다. `--backfill` 은 지금 뜻 그대로 두고 도움말만 코드에 맞춘다. GUI 에 '최근 N일 다시 읽기(이력 보존)' 체크. 실장비 재수집은 이것이 생긴 뒤 `--refresh-window 30` 으로 |
| D61 | 배포 대상 (9/20 확인 4) | 업데이터는 **main 의 그 SHA 에 필수 CI(tests.yml)가 성공한 커밋만** 받는다(조회 불가·pending·실패면 현재 버전 유지). TLS 검증 실패 시 무검증 재시도 폐지, zip 은 `archive/<full sha>.zip` 으로 고정, 페이로드는 허용 목록, 적용은 파일별 저널로 롤백. release manifest 채널은 안정화 뒤 |
| D62 | 보관 (9/20 확인 6) | 옛 스크린샷·데모·옛 샘플·낡은 디자인 문서는 **삭제하지 않고 archive/ 로 이동**(+gzip, 이동 전후 SHA256 확인), 없어진 모듈의 .pyc 만 추적 제외. 진행상황.md 의 옛 기록은 docs/history/ 로 원문 이동 |
| D63 | Rescan 의 뜻 (9/20 확인 7) | **앞선 시도의 결과가 PASS 일 때만 Rescan**(D48-③ 문장 그대로). Skipped·중단·미확인 뒤의 Pass 는 정상 Scan. 30일치 Rescan 15,561건 중 5,797건이 Scan 으로 |
| D64 | Test Lot (9/20 확인 9) | Lot 이름에 TEST 가 들어가면 **그 Report 의 행은 전부 Test** — 원인 Error 가 있어도 Error 건수·시간에 넣지 않는다(D53 보완, CLAUDE.md 의 '오류 건수에도 넣지 않는다' 가 그대로 맞음) |
| D65 | 진행 방식 (9/20 확인 10·11) | 개선 계획은 **단계마다 main 에 합친다**(테스트 전부 통과 + Chromium 확인 뒤). 착수는 확인 답을 모두 받은 뒤(9/20 저녁 모두 받음) |
| D66 | 장비 정체성(9/20 밤, P6 확인 3) | **이름만 같은 장비는 합치지 않는다** — 다른 경로 표기를 같은 장비로 잇는 것은 OS 가 알려 준 UNC 동치와 사용자가 `device_path_aliases` 에 적어 승인한 묶음뿐. 충돌은 새 키 + `identity.conflicts` 로 드러낸다 |

## 실물로 확인한 사실 (근거 있는 것만)

- **Report 폴더 이름이 장비마다 다르다** — `Report`(AOI-4~16) / `Reports`(나머지 17대).
- **Scanresult 경로의 출처는 Report 안의 `Job/Setup`** 이다. 파일명 규칙은 실장비 516개 중 6개만 맞았다.
  `Job/Setup` 이 없는 옛 형식 5대(AOI-1·2·6·10·11, 값이 `/` 로 비어 있음)만 파일명 규칙으로 되돌아간다.
- **`WaferInfo.ini` 는 다시 검사하면 같은 경로에 덮어써진다**(AOI-25 9/14 `00NSP049XYG7`).
  → 첫 시도의 시각은 **복구 불가능**하다. 배치 구간 밖 INI 는 `STALE` 로 두고 시간을 쓰지 않는다.
- **실패한 배치는 Scanresult 에 흔적이 없다** — 아는 시간은 Report 의 `Batch Start~End` 뿐이다.
- **INI 누락의 주원인은 Scanresult 백업 폴더 미조회였다**(9/19 디자인 세션의 30대 조사, `docs/design/dashboard-redesign/data/`): 현장은 어느 날짜 기준 그 이전
  Scanresult 를 통째로 `Scanresult_Back up_260918` 류 폴더로 옮긴다. 16대에 백업, Job 폴더 5,078개. 검사 Lot 4,107 중 1,840(44.8%)이 백업에서 발견,
  읽을 수 있는 INI 24,050 → 53,069(2.2배, AOI-7 0→4,465 · AOI-4 0→1,165). 못 찾은 984건 중 731건은 검수 스크립트 파싱 오류, 실제 결손 75건(2.7%).
  AOI-4 `XBL WBG\24` 처럼 Wafer 폴더에 `MoveResultFlag` 만 있고 INI 가 없는 것은 '이동만 되고 스캔 안 함' 이다. **4층 5대는 백업 폴더가 없다.**
- **디자인 추출 스크립트는 재현된다(9/20)**: `scripts/make_aoi_data.js` 를 원본 `AOI_capacity 3.html`(20MB)에 돌리면 저장소의 `app/aoi-data.json` 과 **바이트 단위로 같다**(2,246,736 B, 2.2초).
  JS 이고 입력은 제품 HTML 의 embedded JSON(`cols/pooled/pool/rows/meta`) 그대로 — 그래서 **template.html 안에 거의 그대로 옮길 수 있다**. README 가 밝힌 RULES.md 와의 차이:
  구간 병합은 데이터 3분 + 화면 8분 두 단계 · 표기명 21개와 추정 판정(50%)은 데이터가 아니라 화면 규칙(추정 = `cv<50 또는 측정 0` 이고 `be>0`) · Lot 이름의 RE 는 남기고 자재 비교에선 뗌 ·
  Test 판정은 원본 Lot 문자열 · 기준 날짜 `day` 하드코딩 · Error 후 대기 cap 240 과 분모 1440 clamp 는 D48 대로 제품 규칙으로 바꿀 두 상수.
- **4층 NOT_FOUND 는 백업 문제가 아니다(30일치 샘플 + 조사 목록, 9/20)**: 4F-AOI-01 Wafer 행 3,272 중 NOT_FOUND 2,391(Report 124개) · 4F-AOI-02 2,263 중 522(39개) · 03/04/05 는 38 · 3 · 10.
  (디자인 조사표의 '4층 0개' 는 그 검수 스크립트의 파싱 오류 — 제품 샘플은 4층에서 EXACT 1,960행을 읽었다.) 01·02 의 NOT_FOUND 는 거의 전부 Report `Job/Setup` 이
  `2D@RE $7781539A-WUP_0858562PD_0A` · `2D@R2-DT-GH10N-BIN1-H-U1_0858092PD_0B` · `2D@RE $7781987B17-WUP_0856808PD_0A` 처럼 **`_0A`/`_0B` 접미가 붙은 Job** 인데,
  조사 목록의 실제 Scanresult 폴더는 `2D@RE-$7781539A-WUP_0858562PD` · `2D@R2-DT-GH10N-BIN1-H-U1_0858092PD` 처럼 **접미가 없다**(하이픈/공백 차이도 있음).
  **사용자가 NAS 에서 확인(9/20)**: `I:\4F-AOI-01\Scanresult\2D@RE-$7781539A-WUP_0858562PD\6412\XAB\SH62L03-E1\WaferInfo.ini` 와 `…\2D@RE-$7781987B17-WUP_0856808PD\6412\FWG` 가 있다.
  → `collect.job_folder_variants`(원문 · `2D@XX `→`2D@XX-` · 끝 `_0A` 뗌 · 둘 다)로 수정. UGK Report 의 NOT_FOUND 2장(GX57511841 Alignment Error · GX57511839 Aborted)은 스캔되지 않아 폴더가 없는 것이 맞다.
  목록: 세션에 남긴 `4F_NOT_FOUND_reports.csv`(174 Report).
- **시각 표기가 두 가지다** — `15-Sep-26 05:31:04 PM`(대부분) / `9/16/2026 1:54:03 PM`(AOI-1·2).
  30대 전수에서 못 읽은 시각 **0건**.
- **`LoadPort A`·`Slot n` 은 Lot·Wafer 가 아니라 자리표시**다. Lot 이 비면 INI 경로에서 칸이 사라져
  **다른 Lot 의 INI** 를 가리킨다.
- **반송 실패 문구가 `… Batch Aborted. Skipped.` 로 끝난다** — 규칙 순서를 잘못 두면 오류가
  '건너뜀(정상)' 으로 묻힌다.
- 30대 전수 샘플 통계: Report 55,717개 · 상태 문구 34종 · Job 96종 · Lot 217종.
- 장비 위치: **AOI-1~25 는 2층, 4F-AOI-01~05 는 4층**(사용자).
- 같은 Lot 안 연속 Wafer 사이 빈 시간(3일치 11,529건): 50% 21초 · 95% 52초 · 99% 1,068초.
  Wafer 교체는 20~30초고, 그보다 길게 비는 것은 설비가 실제로 쉰 것이다 → 막대 나누는 기준 60초.
- **실장비 첫 수집(9/13~9/16, 25대 15,626행)에서 드러난 것**
  - `폴더 *` 행 때문에 **4층 5대가 한 대도 수집되지 않았다** → `*` 가 제한 중에도 도는 길을 만들었다.
  - **AOI-10 의 낮은 가동률은 설비가 쉰 게 아니라 데이터 문제였다.** Setup 이 `SETUP`(4자리 아님)이라
    파일명 규칙이 빗나가 job 이 비었고 INI 경로가 통째로 어긋났다 — 9/15 하루에만 8.8시간이 '미가동'.
  - 미분류(OTHER) 표기 7종: Clean Reference Error.(72) · Nothing to Scan.(7) · Manual Alignment Failed.(6) ·
    Scanning Multi recipe error.(3) · Prealigner failure(2) · Wafer Handling Failure(1) · Failed on MoveToStation(1).
  - INI 를 못 찾은 행은 전체의 2.1%(329행). 그중 절반이 위의 빈 job 탓이었다.
  - 첫 수집에 **28.6분**(동시 읽기 적용 전 빌드).
- **결과 화면 실측(1280·1440, Chromium)**: 막대 클릭·Enter → 상세 패널 고정(원문·복사·INI 확인 상태·Report 목록), ESC 로 닫힘,
  오류 표의 'Report 열기' 버튼이 행 클릭과 겹치지 않음, 390px 폭에서 상단 4탭이 뜨고 추이로 이동됨, 타임라인은 900px 로 유지되어
  가로 스크롤(막대 높이 25.5px). 휴대폰 폭에서는 상단 바가 아직 767px 로 넓다(범위 밖 — D29).
- **확대 타임라인 실측(1440, AOI-8 9/15 02:00~03:00 드래그)**: 막대 폭 중앙값 7px → 96px, 가동률 값은 그대로(54.341%).
  6초짜리 배치 오류는 1시간 구간에서도 2단위 최소 폭(1.9px)이라 포인터 층·Tab 으로 잡는다. 날짜·장비를 바꾸면 확대는 풀린다.
- **결과 화면의 'HTML로 저장' 이 열을 골라 담아 데이터가 깨졌었다**(실데이터로 재현): 17열 중 11열만 저장해
  `kind`·`scan_type`·`report` 가 사라졌고, 그 파일을 다시 열면 TEST 280건이 실가동에 섞여 검사시간 합이 150,204초
  늘고(9/16 평균 56.4%→59.2%) 배치 표시·Report 열기가 없어졌다. 지금은 수집기가 준 열·문자열 풀을 그대로 다시 접는다 —
  저장→재열기 전후 가동률·오류·TEST·배치 수치가 같음을 Chromium(Playwright)으로 확인.
- **같은 파일도 다음 날 열면 오늘(9/16) 평균이 56.37%→43.99% 로 바뀌었다.** D17(오늘만 마지막 스캔까지)의 결과였다 —
  분모가 24시간으로 돌아가기 때문. 문구를 '마지막 스캔 기준' 으로 고쳤고, **D40 으로 '오늘' 을 수집 시각에 고정해 이제는 바뀌지 않는다**(9/18 phase 6 실측).
- **브라우저 실측(현재 template 으로 샘플을 다시 그린 사본, Chromium)**: 타임라인 막대의 반응 영역 = 그려진 영역(여유 0px).
  1440px 창에서 축척 0.95 → 최소 막대 1.9px, 1280px 1.63px. AOI-8 9/15 막대 34개 중 8px 미만 25개. 오류 막대가 Lot 위에
  그려져 2px Lot 중앙을 가리켜도 오류 툴팁이 뜬다(3건). 툴팁 5~7줄·중앙값 178자·최대 281자(마지막 줄이 파일명 전체).
  820px 미만에서는 사이드바가 사라지고 대체 메뉴가 없다. 3일치 파일 열기 ~320ms · 날짜 이동 14ms — 화면 성능은 병목이 아니다.
  → 타임라인 맨 위에 투명 포인터 층을 두어 가장 가까운 막대(≤6px)를 잡는다. 다시 잰 결과(같은 두 날): 막대 중앙에서
  자기 자신 33/34, 오류 막대와 그 Lot 이 겹친 곳은 오류가 먼저 뜨고 Tab 으로 Lot 로 넘어간다. 툴팁은 3줄+안내 한 줄,
  중앙값 88자·최대 135자(파일명 없음), 화면 밖 0건. 막대 폭·시간 길이는 그대로다.
- **INI 읽기는 행마다 `isfile`+`open` 2회 왕복이었고 실행 안 메모가 없었다** — 3일치 후보 13,920건 중 같은 경로 반복 1,090건.
  → 이제 존재 확인 없이 바로 열고(없으면 open 이 알려 준다), 한 실행 안에서 같은 경로는 한 번만 연다(`_IniMemo`).
  단계별 시간·읽은 수·INI 요청/실제 읽기 수를 `meta.timing` 에 담아 결과 화면 설정 탭 '수집 시간' 표에 보여 준다 —
  **실장비에서 아직 재지 않았다**(다음 수집 때 이 표가 첫 기준선).
  캐시는 Report mtime 만 보므로 파서를 고쳐도(acaa6ce) 옛 행(AOI-10 NOT_FOUND 153행, 상태 재분류 93건)은 재수집해도 안 바뀌었다
  → 이제 캐시를 읽을 때 규칙 번호가 다르면 상태·검사 종류를 다시 계산하고(NAS 접근 0), INI 를 못 찾은 Report 는
  '시간 미확인 Report 다시 읽기' 를 켜면 mtime 과 상관없이 다시 읽는다(STALE·자리표시·실패 배치는 대상 아님 — 되돌아오지 않는다).
- 수집기의 장비 '완료(초록)' 는 **읽기가 끝났다**는 뜻이었지 성공이 아니었다(Report 가 전부 깨져도 초록). 이제
  '일부 실패'(주황)를 나누고, 완료 안내에 접근 못 한 장비·일부 실패 장비 이름을 적으며, 결과 화면 홈 카드에
  `수집 실패`·`일부 누락` 칩이 붙는다(`meta.devices[].status`).
- **HTML 은 수집할 때마다 통째로 다시 쓴다**(데이터가 그 안에 박혀 있다). 실측: 3일치 1.0MB·0.15초 /
  30일치 9.8MB·0.60초 / 90일치 29.3MB·1.72초, 브라우저에서 열리는 데 2.1초(90일치). 다시 쓰는 비용은
  NAS 읽기에 비하면 반올림 오차다. `file://` 페이지는 `fetch` 가 막혀(크롬에서 확인) 데이터 파일을
  따로 두는 구조는 로컬 서버 없이는 불가능하다 — `<script src>` 로 쪼개는 길만 남아 있다(아직 안 함).

- **9/17 인계 프롬프트(ChatGPT 작성)의 주장을 먼저 저장소 샘플(3일치 25대 15,626행)로, 그 뒤 실제 첨부(`dev/samples/AOI_capacity_2026-09-17_30대.html`, 30대 18,719행)로 확인했다.**
  브라우저 실행은 Chromium(Playwright)으로 했고 NAS 는 건드리지 않았다. 아래 첫 묶음은 3일치 샘플, 두 번째 묶음이 첨부 기준이다.
  - 자재 키(Lot 토큰 제외 + Wafer ID) 12,407개 중 두 번 이상 나온 것 1,235개, **다른 장비에 걸친 것 299개**, 같은 장비에서 시각이 다른 실제 반복 81개.
    같은 자재의 Recipe 가 장비마다 다르다(AOI-3 `BS` ↔ AOI-16 `BS_1`) → Recipe 불일치를 일괄 '다른 공정' 으로 보지 않고 **추정**으로 둔다.
  - 새 화면 분류 결과(전체 로드 기준): 다른 장비 재스캔 296 · 같은 장비 재스캔 252 · 중복스캔 766 · 보류 0.
  - **옛 템플릿 ↔ 새 템플릿 같은 행으로 비교**(장비·날짜별 전수): 총 가동시간·평균 가동률은 4일 모두 **소수점까지 같다**
    (9/13 14.799% · 9/14 51.769% · 9/15 57.344% · 9/16 43.990%). 달라진 것은 셋뿐이고 이유가 있다 —
    ① 자정을 넘는 Wafer/Error 를 **시작일에 한 번만** 세어 nWafer 가 장비별 −1, 9/15 Error 건수 144→142(AOI-24·25).
    ② 복구 기록 없는 마지막 Error 의 정지(추정)를 **관측 종료(9/16 21:01)까지만** 세어 9/16 정지 62,585→24,617초(AOI-7·14·20 — 전에는 자정까지 4시간을 정지로 셌다).
    ③ Test 시간(testRun)이 표시 항목으로 드러남(전에도 미가동에 넣지 않았다).
  - 가동률 저하 사유 %p 합 + 평균 가동률 = 100.0(9/15: 57.3 + 42.7). 사유 상세의 이벤트 합 = 사유 시간(정지 14h57m = 37건 합).
  - **사본 저장 → 재열기**(같은 기준시각): 행 15,626 · 열 17 · 장비·날짜별 가동/Error/정지/Test/중복스캔/재스캔/Rework 수치 · 시도 13,628건의 분류 · Report 링크 300/300 전부 같음. 사본도 바깥 요청 0건.
  - **화면 실측(Chromium)**: 상세 패널이 첫·중간·마지막 카드 모두 그 버튼 바로 아래(간격 10px, 사이에 다른 장비 없음, `aria-expanded` 갱신), 표 보기는 바로 다음 행(colspan 6), `#detail` 은 문서에 하나.
    사유 행 높이 44px(1280·1440·800px), 800px 폭에서 상단 탭·가로 스크롤 없음. 키보드: Enter 로 사유·장비 열림, Esc 로 닫히고 포커스가 그 버튼으로 복귀.
    사유 이벤트 클릭 → 그 장비 상세 + 그 행 고정 → '사유 분석으로' 복귀 시 선택 사유 유지. 드래그 확대 뒤 잘못 열리는 상세 0건.
    타임라인(AOI-8 9/15, 축척 0.95/0.817): 막대 45개 중 반복 시도 조각 11개 모두 중앙 hover 에서 자기 자신이 잡힘(11/11), 최소 폭 1.9px(1280: 1.63px) 그대로.
    반응 시간: 날짜 이동 15ms · 장비 상세 39ms · 지문 계산(전 장비 4일) 83ms — 18,719행 첨부 기준 측정은 아니다.
  - 실측으로 잡은 버그 둘: 표 보기에서 카드 DOM 이 남아 같은 id 때문에 상세가 숨은 쪽에 붙던 것, `.btn` 의 inline-flex 가 `hidden` 을 이겨 '사유 분석으로' 가 늘 보이던 것.
- **첨부 30대 18,719행 기준(sha256 `58b70556…`, 첨부의 JS 는 옛 template 과 완전히 같음, 현재 규칙으로 재계산해도 달라지는 행 0)**
  - 옛 ↔ 새 템플릿 장비·날짜별 전수 비교: 총 가동시간·평균 가동률 5일 모두 같다(9/13 14.799 · 9/14 46.831 · 9/15 50.730 · 9/16 53.392 · 9/17 60.095%).
    달라진 것은 9/15 Error 건수 157→155(자정 넘는 건을 시작일에 한 번) 하나. 정지(추정)는 5일 모두 같다(관측 종료 9/17 08:25 가 마지막 날 안이라 cap 이 걸리지 않음).
  - 단위를 나눠 적은 전후: **행** 18,719 · 시각 미확인 행 1,672(STALE 1,168 + NOT_FOUND 504, 시간으로 바꾸지 않음) · 시각 있는 막대 14,704 중 **같은 시각을 Report 2개가 참조 49개**(행 14,753 → 시간 49건 한 번만, 재스캔으로 칠하지 않음) · 같은 원본 행 중복 0.
    **시도** 16,289(시각 있음 14,617): 확인된 첫 기록 14,348 · 중복스캔 1,002(확정 964·추정 38) · 같은 장비 재스캔 303 · Lot RE 표기(앞선 기록 없음) 265 · **다른 장비 재스캔 371(확정 111·추정 260)** · 보류 0.
    옛 화면에서 '재검사(노랑)' 였던 것은 RESCAN 표기 407행 + 같은 시각 참조 49건이었고, 새 화면의 재스캔은 관계 기준이라 수가 다르다 — 라벨만 바뀌었고 시간 합은 그대로다.
    **시간**(9/16 하루, 27대): 가동 345.9h 중 중복스캔 25.8h · 재스캔 22.0h · Rework 3.3h · Test 14.8h(분모 포함·분자 제외) · Error 2.9h · 정지(추정) 40.0h · 미가동 244.3h · 시간 충돌 0초.
  - 사유 %p 합 + 평균 = 100.0 (9/16: 53.392 + 46.608). 9/17(오늘 분모)은 산술평균 60.09% 와 합산 시간 기준 59.16% 가 다르고 둘 다 화면에 적힌다.
  - 사본 저장 → 재열기: 행 18,719 · 열 17 · 수치·시도 16,289건 분류·Report 링크 전부 같음. 외부 요청 0건.
  - 성능(Chromium, 이 환경): 파일 열기 0.46초 · build 124ms · 전 장비 5일 dayMetrics 112ms · 날짜 이동 53ms · 장비 상세 45ms · 정렬 4ms · 사유 상세 60ms. 90일 규모는 재지 않았다.
  - hit(AOI-8 9/16, 막대 73개 중 반복 조각 43개): 처음 40/43 — Wafer 한 장짜리 Lot 은 Lot 막대와 조각 폭이 같아 Lot 이 먼저 잡혔다 → 폭이 같으면 조각 우선으로 고쳐 43/43. 최소 폭 1.9px(1280: 1.63px) 그대로.
  - 상세 위치(첫·중간·마지막 카드, 표 다음 행)·44px·키보드·800px·드래그 가드는 3일치와 같은 결과.
- **중단(Abort)을 Error 에서 뺀 효과(D35, 같은 첨부로 전후 비교)** — **가동률·가동시간은 5일 모두 한 자리도 바뀌지 않았다**(중단은 전에도 분자 밖이었다).
  바뀐 것은 Error 건수와 정지(추정)뿐이다. abort 는 Error 행 1,255개 중 731개(ABORTED 647 + USER_ABORT 84)였고,
  그중 시각이 있는 것은 51행뿐이라 시간 자체는 작지만 **그 뒤에 붙던 '정지(추정)' 가 컸다**.

  | 날짜 | Error 건수 | 정지(추정) | 중단 시간 |
  |---|---|---|---|
  | 9/14 | 150 → 93 | 20.6h → 15.5h | 1.1h |
  | 9/15 | 157 → 81 | 15.8h → 13.2h | 0.7h |
  | 9/16 | 119 → 63 | 40.0h → 18.4h | 0.5h |
  | 9/17 | 69 → 45 | 6.2h → 4.6h | 0.1h |

  줄어든 정지 시간은 사라지지 않고 '미가동 · 사유 미확인' 으로 옮겨 갔다(9/16 미가동 244.3h → 265.9h). 사유 %p 합 + 평균 = 100.0 은 그대로다.
  타임라인에서 중단은 회청색 교차 빗금으로 남고, 장비 제목에 '중단 n건' 칩이 붙는다. 9/16 AOI-14 는 Error 7건·정지 3h32m 에서 Error 2건·정지 0m·중단 5건이 됐다.

- **30일치 첨부(8/19~9/18 · 156,109행 · Report 12,665 · 상태 문구 213종)로 실측한 것(9/18, 원장 phase 0~8)**
  - `Failed to read wafer id … Wafer Skipped.` 192행(89 Report)이 '건너뜀' 에 묻혀 있었다 → 원인 ID_READ_ERROR + 결과 SKIPPED(D43). 미분류(OTHER) 38행은 기술 원인 35 · 취소 1 · `-` 2 로 전부 갈렸다.
  - 원인 없는 중단을 Report 의 PASS 유무로 나누면(D41) Error +517건 · Error 구간 +31.8h · 정지(추정) +753.8h(미가동에서 옮겨 감) · 가동 +7.8h(정상 스캔 있는 Report 의 중단 126시도).
  - 같은 INI 시각을 여러 Report 가 참조한 묶음 413(상태 다른 259) 중 412 가 엄격 포함 Report 하나(소유자 상태 PASS 409) → 시간 잃은 행 415, Error 구간 −10.5h → 가동, 정지(추정) −85.2h → 미가동(D37).
  - 자리표시 행 Error 가 있는 부분 실패 Report 125 = Slot 사건 103 + 새 실패 배치 22(검사된 Wafer·PASS 없음). Slot 사건은 시간 0 기여(D38).
  - Job 원문을 공정으로 쓰면(D36) 다른 장비 재스캔 20,788 → 1,119, 확인된 첫 기록 100,476 → 121,375, 보류 4 → 1(override 1건 적용) — **가동시간·가동률은 불변**. 자재 키 정규화(D39)로 합쳐진 키 1,835(작업 표기 1,328 · Lot 구분자·대소문자 502 · Wafer 4).
  - 기준시각을 수집 시각에 고정하면(D40) 9/25 · UTC 로 열어도 전 수치 동일. 사본 저장 → 재열기: 가동률·Error·사건 3,319·분모 132,713·수집 시각 전부 같음(합성 slot 은 다시 만들어짐).
  - Chromium: 열기 3.2~3.3초(19.5MB) · Error 분석 탭 전환 p95 300ms · 콘솔 오류 0 · 외부 요청 0 · 1280/1440/1920 · 125~150% · 800px 가로 스크롤 없음.
  - 화면 성능 병목은 필터 줄(Job 533종 목록)과 15만 행 재훑기였다 — 캐시·lazy 로 해결. 원인 규칙을 바꾸면 `status_mapping_2026-09-18.tsv` 와 어긋나는 문구가 테스트에서 드러난다.

- **현장 10일치(9/20 17:18 수집, `--rebuild-all`, 45분, 30대 · 56,507행 · Report 4,388개 읽음)**: 안정 키 이관 `identity 12,724 · superseded 0 · 충돌 0`, `issue_codes` 13,964행(NO_WAFER_ID_ROW 5,936 · INI_STALE 4,025 · FOUND_IN_BACKUP 1,251 · JOB_FOLDER_DIFFERS 1,001 · INI_NOT_FOUND 1,297 · MOVED_ONLY 101), `faults` 56,169행 전부 채워짐.
  **시각 확인(EXACT) 79%** — 조사 시점과 비교: AOI-4 4%→74% · 4F-AOI-01 15%→81% · AOI-7 43%→79% · AOI-5 53%→83% · AOI-12 61%→87% · AOI-2 66%→83%(백업 폴더 조회의 효과). 낮은 쪽: **4F-AOI-03 14%**(행 36개 중 실패 배치 23 — INI 문제가 아니라 거의 돌지 않은 장비) · AOI-23 61%(STALE 196, INI 덮어써짐).
  10일치 평균 가동률 59.0%(수집일 63.2%). **보관 기간 첫날 9/10 은 20%** — 수집 창이 17:18 부터라 하루 전체가 아니다(화면이 '부분' 으로 표시하고 추이 평균에서 뺌). 결과 HTML 의 버전 표기가 `v9be6d0d`(9/17 코드) 였다 — 코드는 새것인데 `VERSION` 파일이 옛 빌드 값이라 그렇다(git HEAD 우선으로 고침).
- **옛 샘플 두 장은 30일치의 내용상 부분집합**(P5 대조: 열 이름 17개 동일, 상태 문구 40·47종 ⊂ 213종, ini_match·kind·scan_type·time_basis·시각 표기·장비 이름에 30일치에 없는 값 0). 그래도 지우지 않고 gzip 보관(D62, 원본 sha 는 `test_sample_rows.py`).
- **CI 미러(Linux, PyQt6 설치)에서 창 닫기가 qFatal 로 죽었다** — 계획 워커를 `deleteLater` 한 뒤에도 참조가 남아 `closeEvent` 의 `isRunning()` 이 RuntimeError. Windows 현장에서도 계획 조회가 끝난 뒤 창을 닫으면 같은 길이었을 것(P1 통합에서 고침).

## 손에 있는 현장 데이터

`dev/samples/AOI_capacity_2026-09-16_3일치.html` — 첫 현장 수집(9/13~9/16 · 25대 · 15,626행).
`dev/samples/AOI_capacity_2026-09-17_30대.html` — 30대 5일치(18,719행).
`dev/samples/AOI_capacity_2026-09-18_30일치.html.gz` — **30대 30일치**(8/19~9/18 · 156,109행 · Report 12,665개 · 상태 문구 213종). 9/18 실행 계획의 입력.
**NAS 없이 이 파일들로 화면을 다시 그려 볼 수 있다.** 꺼내는 법(`dev/tests/sample_rows.py`)·집계 도구(`dev/tools/measure.py`)·한계는 `dev/samples/README.md` 에,
단계별 실측 전후표는 `dev/samples/ledger_2026-09-18.md` 에 있다.

## 다음 할 일

00000000. **수집 가속 실장비 확인(9/23 구현)** — 현장 PC 에서 수집 두 번: ① 첫 번째(전체 나열) 로그 끝 `단계별 경과` 줄 ② 몇 시간 뒤 두 번째(이름 패턴 나열) 같은 줄 — `목록 …ms` 가 수 초로 줄었는지, `이름 패턴 나열 n대` 가 30 근처인지, 장비 줄에 `최근 3일 이름으로 거른` 이 보이는지, 경고 `이름 패턴 나열 실패` 가 없는지. 읽기는 `--rebuild-all` 한 번으로 예전 79분 대비를 본다(NAS 별 동시). 느려지면 설정 'NAS 마다 동시에 읽기' 를 4 로.
0000000. **개선 계획(Codex JSON, 51+2 항목, 7단계 P0~P6) 실행 — 9/20 저녁 착수.** 결정 D56~D65. 단계마다 커밋 + main 합침(D65). 단계별 상태는 작업 기록에.
   - [x] P0 기준선(`037db9f`) → [x] P1 보호(N02 · S01/S17 · C01 · C10 · C15 · C02/D60 · C06 · S02/S04/S10/S16 — worktree 3개 병합, 9/20 저녁, `7d47f27`) → [x] P2 모델(D09 · N01/D63 · D04 · D01/D02/D56~D58; I02 변화 원장은 `dev/tools/measure.py` + 원장으로 대신) → [x] P3 수집(C03 · C04 · C05 · C07 · C08 · C09 · C12 · C13 · C14 · C16 · D14 — worktree 2개 병합, 9/20 밤) → [x] P4 화면(D05~D08/D59 · D10 · D11 · D13 · D14(화면 쪽) · D15 · D16 · S06 — 9/20 저녁, Chromium 실측) → [x] P5 정리(S03 · S05 · S07 · S08 · S09 · S11~S15 · S18 · C11 · D15 — D62 보관, 9/20 밤) → [x] P6 검토 패키지(9/20 밤 — 확인 아티팩트 'AOI 개선 계획 검토', 확인 항목 7개 · CI run 1~4 초록)
   - 남은 확인(실장비·Windows 에서만 가능): ① 첫 실행의 옛 캐시 → 안정 키 이관 로그(`identity` 줄, 충돌 0 이어야 정상) ② `--rebuild-all` 뒤 `issue_codes`·`faults` 채워짐 ③ pythonw 의존성 실패 안내 · 작업 스케줄러 종료 코드 · bat 로그 회전 ④ 자동 업데이트가 CI 초록 SHA 를 받는지(보류 문구 포함) ⑤ 평균 가동률(모델 v3 57.2%)이 현장 감각과 맞는지.
000000. **전체 감사 개선 항목 51개** — `docs/audit/improvements_2026-09-20.json`(D=화면 · C=수집기 · S=테스트/빌드/문서, 권장 순서 포함). 먼저 할 것: D01·D02(모델 겹침 483h · Error 후 대기 과산정 — 규칙 확정 필요) → S03·D08·D09·D16 → S01·C01·S02(배포 안전).
00000. ~~이식 검수 확인 항목 답 반영~~ → 9/20 D52~D55 로 반영·합침. 남은 것: **확인 4(평균 가동률 59.6% 가 현장 감각과 맞는지)** 는 실장비 `--full` 재수집 뒤 화면에서 본다.
0000. ~~D47 이식(P0~P5)~~ → 9/20 완료(`9ecaa47`, 원장 `Phase D47`). 항목 원문은 `docs/history/진행상황_2026-09-13~19_기록.md`.
000. **실장비 적용(사용자 실행 대기, 9/20 밤 P6 답 '모두 권장')** — 순서: ① 현장 PC 갱신(update_code.bat 또는 자동 업데이트, 보류 문구 종류 보고) ② `aoi_cache.json` 복사 → GUI '수집' 한 번 → 로그 identity 줄 충돌 0 확인
   ③ ~~`--rebuild-all`~~ ④ ~~결과 HTML 한 바퀴~~ → **9/20 밤 10일치 HTML 로 검수 완료(실물 사실 참조)** ⑤ Windows 세 가지(pythonw 안내 · 스케줄러 종료 코드 · bat 회전) — 아직 ⑥ 새 화면(morph · 스택 · 필터 · 정렬)을 현장에서 다시 열어 보고 깜박임이 사라졌는지 확인.
   (옛 항목) 백업 폴더 조회 적용 후 실장비 재수집 — `--rebuild-all` 로 다시 읽고 장비별 **시각 확인 비율** 전후를 적는다
   (조사 시점: AOI-4 4% · 4F-AOI-01 15% · AOI-7 43% · AOI-5 53% · AOI-12 61% · AOI-2 66% / 정상군 AOI-15 87%). 캐시 행에 `faults` 도 이때 채워진다.
   수집 로그의 `[AOI-n] Scanresult 백업 폴더 k개도 조회합니다` 줄로 폴더가 잡혔는지 먼저 본다.
00. ~~9/18 실행 계획 phase 0~8(D36~D46)~~ · ~~Codex 인계 구현 묶음 ①~⑥~~ · ~~9/17 인계(D30~D34)~~ → 전부 완료(9/16~18). 원문은 `docs/history/진행상황_2026-09-13~19_기록.md`. 보류로 남긴 것: 중앙 수집·NAS 인접 호스트·변경 목록·자동 조절·SQLite/Worker(사용자: 한 PC 수동 수집·HTML 공유) · phase 9 고급 품질 탐색은 개선 계획에 흡수.
1. **고친 것을 실장비에서 다시 확인** (사용자 실행 대기) — `python scripts\update_code.py` 뒤 재수집.
   - 4층 5대가 이번엔 잡히는지(`폴더 *` 행 그대로 둬도 된다).
   - **AOI-10 의 `R_TB500 TOP D-DIE_0860312PD` 배치들이 INI 를 찾는지** — 9/15 가동률이 24% → 60% 대로
     올라가야 맞다. 여전히 NOT_FOUND 면 그 Job 의 Scanresult 폴더 이름이 다른 것이므로 실물 확인이 필요하다.
   - 동시 읽기 8개로 수집 시간이 28.6분에서 얼마나 줄었는지 — 설정 탭 '수집 시간' 표(단계별 시간 · INI 요청→실제 읽기)를 적어 둘 것.
     첫 수집 · 변경 없는 재수집 · 누락 복구를 따로 잰다.
   - 더블클릭 Report 열기가 실제 NAS 경로에서도 되는지.
2. 배포(exe-lite) 현장 설치 — 아직 한 번도 안 했다.

## 확인 대기 (답이 오면 코드에 반영)

D36~D46 으로 Q01~Q03 · 정지(추정) cap · 중단 · Enter · 분모 질문은 모두 해소됐다. 남은 것은 **실행을 막지 않는 선택적 후속 확인**이다(다시 묻지 않고 구현한다). 해소된 RQ1~RQ7 · DQ1~DQ5 · 4F NOT_FOUND 의 원문은 `docs/history/진행상황_2026-09-13~19_기록.md`:
- **확인 4(RQ7)** — 평균 가동률(30일치 57.2%, 모델 v3)이 현장 감각과 맞는지는 실장비 `--rebuild-all` 재수집 뒤 화면에서 본다.
- ~~P6 검토 아티팩트(9/20 밤) 확인 7개~~ → **9/20 밤 사용자 답: 모두 권장대로.** 현장 순서(아래 다음 할 일 000)와 10일치 HTML 업로드 대기. 원문: ① 첫 실행 전 `aoi_cache.json` 복사 뒤 증분 수집으로 안정 키 이관(로그 identity 줄 충돌 0) ② `--rebuild-all` 은 바로 ③ 같은 이름·다른 경로 장비는 합치지 않음(승인은 `device_path_aliases`)
  ④ 자동 업데이트 첫 동작을 현장 PC 한 대에서 먼저(보류 문구 종류 보고) ⑤ Windows 만: pythonw 안내 · 스케줄러 종료 코드 · bat 회전 ⑥ 평균 가동률 감각은 재수집 뒤 ⑦ 작은 정리(`_in_batch_window`·`meta.limit` 삭제 · Actions v5 · 캐시 검증 메모리 386MB)는 다음 커밋.

- **OQ1** 짧은 숫자 Wafer ID 를 가진 자재 키 병합 후보의 원문 쌍 검토 — D39 대로 합쳤고 목록은 `dev/samples/material_merge_2026-09-18.tsv`(합쳐진 키 1,835 중 짧은 숫자 Wafer ID 후보 188, 예: `UXD-#1`·`UXD-RE #1`·`UXD #1 RE` ⟂ `1`).
- **OQ2** 보류 3건(AOI-9 YCG-EDGE GWM5K08-D1 · GWM5K07-A6 · AOI-15 KDS-DIA GX57501413)의 실제 순서 — 확인해 주면 `USER_OVERRIDES` 에 더한다. D36(Job 원문) 적용 후 이 3건은 Job 이 달라 자동으로는 잇지 않는다(보류 표시가 아니라 '이 Job 의 첫 기록').
- **OQ3** 이름이 조금 다른 Job 을 같은 공정·제품군으로 묶는 현장 기준이 있는지 — 있으면 선택형 그룹 보기를 더한다. 지금은 원문 Job 별 분석만.
- **OQ4** `4F-AOI-03·04·05` 는 Report 가 8~23개뿐(셋업용?) — 지금처럼 일반 카드(D12·D25).
- **OQ5** 장비 시계 오차의 실제 크기 — 측정값이 없어 보정하지 않는다. 순서 override 만 별도 필드로 보존.

## 작업 기록

새 항목을 **맨 위**에 넣는다. 형식: `- <날짜> <커밋> — 무엇을·왜`

지금 만드는 커밋의 해시는 아직 없으므로 `(작업 중)` 으로 적고, **다음 커밋 때 그 자리를 채우면서**
자기 항목을 `(작업 중)` 으로 새로 올린다(`git commit --amend` 로 채우면 해시가 또 바뀌어 헛돈다).

- 2026-09-23 `(작업 중)` — **수집 가속 두 가지**(사용자: 여전히 느리다 → 제안 ①② 적용, "X·M·V·P·Y·I 는 서로 다른 NAS" 확인). ① **NAS 별 동시 읽기**: `_run(group=nas_group)` 이 장비 나열·Report 읽기를 NAS(UNC 호스트 · 드라이브 문자)마다 `read_workers` 개씩 번갈아 돌린다(전체 64 상한, 풀 하나, 입력 순서 결과 · 가장 앞선 예외). 9/18 30일치 실측의 스레드 시간을 드라이브별로 나누면 X 26분 · M 11분 · V 31분 · P 9분 · Y 2분 · I 1분(8개 기준) — 예전엔 합이 79분, 이제 가장 느린 V 기준 **약 31분 추정**(실장비 미측정). ② **Report 목록을 이름 날짜 패턴으로**: 커서 있는 장비의 증분 수집은 `*_26-Sep-23_(*` 같은 패턴(커서 전날~내일)을 `nas_guard.find_pattern`(Windows `FindFirstFileExW` + LARGE_FETCH)에 넘겨 NAS 가 거른 파일만 받는다 — 고르는 규칙은 그대로, 받는 목록만 부분집합. 처음·backfill·refresh·recover·rebuild·14일 초과·패턴 실패·마지막 전체 나열 20시간 초과(캐시 `full_listed`, 새 저장 이유 `listing`)는 전체 나열이라 `EXPORT.htm` 같은 이름 밖 파일도 하루 안에 잡힌다. 목록 5.5분 → 수 초 추정(실장비 미측정). 로그 끝줄에 `NAS n대 × 동시 k개 · 이름 패턴 나열 m대`. 설정 문구 'NAS 마다 동시에 읽기'. `find_pattern` 의 ctypes 경로는 Linux 에서 돌릴 수 없어 **Windows 실행 확인 전**이다 — 실패하면 전체 나열로 되돌아간다(경고 로그). 가드 `test_collect_listing.py` 8개.
- 2026-09-23 `c5ab5ac` — **Error 탭 목록의 글자도 숫자처럼 넘어간다**(사용자: "글자가 번쩍하고 바뀌는 게 아니라 숫자들처럼"). 흐림→또렷(`tx-in` 키프레임)을 지우고, `data-tx` 글자는 0.24초 동안 왼쪽부터 한 글자씩 새 글자로 바뀌며 경계 두 칸만 같은 종류 글자(숫자·대문자·소문자)로 넘어간다(`txMix` — 위치·프레임으로 고르는 결정적 순환, 같은 글자는 그대로라 AOI-16 → AOI-19 는 끝 한 칸만). 30일치 실측: `AOI-19 → 4K--19 → 4F-E19 → … → 4F-AOI-02`, 줄 트윈 0 · transform 0 유지. 브라우저 테스트는 끝 글자 정확·넘어가기 1회·CSS 애니메이션 0 을 본다.
- 2026-09-23 `a3110be` — **Error 탭 목록은 제자리, 글자만 바뀐다**(사용자: 날짜를 바꾸면 아래 목록이 아래에서 올라오며 출렁여 불편 → 비교 페이지의 3안 중 "2안 막대만 스르륵" + "글자 바뀌는 애니메이션" 선택). 유형별·장비별·Job별 세 목록의 줄 키를 이름(`dev:`·`type:`·`job:`)에서 순위 칸 `slot:i` 로 바꾸고(실제 대상은 `data-row`) 패널의 `data-anim` 을 뗐다 → Flip 이동·아래에서 올라오는 등장·사라지는 퇴장 없음. 숫자(`data-txn`)는 이전 값에서 0.2초 세고, 이름·Lot 줄(`data-tx`)은 흐림→또렷 0.22초(`txAnim`), 막대 폭 0.2초(`.errlist`). 처음 나타날 때는 연출 없음. 30일치 샘플 Chromium 실측: 9/18→9/16 전환에서 줄 노드 31/31 유지 · 줄 트윈 0 · transform 0, 숫자 4→2건 · 8시간 3분→14시간 57분 세기 뒤 정확. 브라우저 테스트 `test_error_lists_keep_rows_in_place_and_only_text_changes` 추가(12 통과), 전체 스위트 통과. 같은 대화에서 수집 속도 분석(9/18 30일치 `meta.timing`: 목록 5.5분 · 읽기 79분, 스레드 8개가 장비 순서로 한 드라이브에 몰림 · 증분 수집의 약 2/3 가 목록)을 답했고 구현은 사용자 선택 대기(다음 할 일).
- 2026-09-21 `47d03eb` — **애니메이션 전수 감사**(사용자: 전체적으로 점검해 과도한 것을 고쳐라). `pw_audit.js` 로 42가지 클릭마다 도는 CSS transition/animation·GSAP 트윈을 대상별로 셌다. 고친 것: 내비 표시자 렌더마다 트윈 → 자리 바뀔 때만 · `.callout`·히트맵 칸의 transform transition 제거(GSAP clearProps 뒤 이중 동작, 추이 진입 시 트윈 330 + transition 330 → 행 30) · 대화상자 등장/퇴장 CSS 키프레임(`.dlg` transition 과 충돌) · 스택 바 칩 등장 제거 · 맥동 1회 · Flip .26s. **morph 버그 2건**: 키 없는 형제 맞추기가 앞 형제 하나가 빠지면 뒤 형제 전부를 재생성(→ 같은 태그를 6칸 앞까지 탐색), 스택 순서가 바뀌면 stackbar 자리에 dlg 가 맞춰져 대화상자 통째 재생성(→ `.dlg`·`.stackbar` 에 data-key). 계측: 팝업 안 장비 이동·Lot 필터 새 노드 0(전 12~115), 리포트 펼침 트윈 1. 브라우저 11 · 계약 · JS 하네스 통과.
- 2026-09-21 `7d6466c` — **틀은 그대로 ② — 진짜 원인 제거**(사용자가 af7f35e 로 만든 HTML: 탭 바 전체가 흔들림 · TB500 탭에서 뭐 하나 눌러도 전체 창 애니메이션 · 팝업 뜰 때 뒷배경 재생성 느낌 · 팝업 간 이동 번쩍임). 계측(`pw_flip.js`): **`#app` 루트·`.stage` 에 걸린 Flip 이 클릭마다 header·본문·바닥글에 1~4px translate 트윈**을 주고 있었다. 루트·무대는 `data-anim="el"`(들고남만), 목록은 키 순서가 바뀌거나 행이 들고날 때만·3px 이상 움직인 행만 Flip, 무대 물러남 scale 제거(밝기 .97만), 이름표·표식 순차 등장은 처음 한 번, 막대 날짜 바뀜 흐림·앞으로 온 팝업 번쩍임 제거, `viewDir` 렌더 뒤 초기화. 계측 후: header/stage/foot transform 0, 리포트 펼침·팝업 열기·팝업 안 장비 이동에 transform 0, 클릭당 트윈 39→9. 브라우저 11 · 계약 · JS 하네스 통과.
- 2026-09-20 `af7f35e` — **틀은 그대로, 내용만**(사용자: 버튼마다 전체 창이 재생성되며 번쩍인다). 계측(`pw_probe*.js`, 현장 10일치): header·main·패널·키 있는 행은 클릭마다 같은 노드(재생성 0) — 보이는 것은 화면 전체가 되감는 연출이었다. 카운트업은 카드에서 값 바뀔 때만, sweep 은 막대가 처음 그려질 때만(날짜 바뀜은 CSS `.swap` 흐림), 차트 키 고정(`chart:errors`·`chart:trend`)으로 막대가 제자리에서 transition, 자리 바뀐 행 번쩍임·날짜 넘김 목록 밀기 제거. 같은 요소에 GSAP 트윈을 겹치면 `clearProps` 가 건너뛰어 clip-path 가 남는 것을 실측해 CSS 로 바꿨다. 브라우저 11 · 계약 · JS 하네스 통과. **확인 대기: 사용자가 만든 HTML 의 바닥글 `v<7자리>` 가 어느 커밋인지**(CI 는 main 전부 성공이라 업데이터가 최신을 줄 수 있는 상태).
- 2026-09-20 `7ff5fe0` — Error 탭 날짜별 막대: **기간 모드(7일·21일·전체)는 모든 날을 같은 빨강**, 일자별 모드만 고른 날 진하게·나머지 연하게(사용자 확정 — 예전엔 기간 모드에서도 수집한 날만 한 단계 진했다).
- 2026-09-20 `a23d64b` — **애니메이션 5차 · 차트 색의 깊이**(사용자: 더더 적극적으로, 차트 색이 단면적). 색은 dataviz 규칙대로 — 막대 색(background-color)은 그대로 두고 위가 밝고 아래가 살짝 어두운 한 겹(background-image)만 얹었다(`.chart .f` · `.hbar i` · `.tline i` · 24시간 막대의 두 번째 gradient 층), 24시간 막대의 색 구간 사이 1px 표면 간격(0.6% 이상 구간만), 히트맵은 한 색조(RUN) 6단 OKLCH 램프(`HEAT`, 3px 간격·둥근 칸·호버 확대), 추이 막대는 기준 미만만 상태색(ERR) 나머지는 RUN(값에 따라 세 색으로 갈라 칠하던 55% 단계 제거, 막대 폭 72px), `.hbar` 트랙은 같은 램프의 옅은 단(crit-soft). 움직임: 탭 전환이 내비 순서 방향으로 가로 미끄러짐(`state.viewDir`, `.stage{overflow-x:clip}`), 카운트업은 처음 나타날 때 0 부터, 히트맵 격자 물결(`stagger grid`), 정렬·필터로 자리가 바뀐 행은 잠깐 파랗게(`moved`), 날짜 넘김에 목록도 방향대로 밀림, 버튼 누름 `:active` 축소, 막대·칸·화살표 호버 반응, 주의 장비 점 맥동 3회, 앞으로 온 팝업 `.dlg.pop` 밝기 번쩍, 덱 깊이당 130px, Error 탭 유형별 빈 상태 Lottie 체크. **막대 등장을 height → scaleY 로 바꿨다**: `gsap.from(height:0, clearProps:height)` 가 inline `height:%` 까지 지워 등장 뒤 막대가 2px 로 주저앉던 잠복 버그(현장 10일치 파일로 실측). 브라우저 테스트 11 · 전체 스위트 통과. 비교 아티팩트는 팝업 덱만 남기고 미니 데모의 inline transform 버그(GSAP `clearProps:"opacity"` 만 지워 CSS 물러남을 덮음)를 고쳤다 — 결과 HTML 은 처음부터 transform 도 지우고 있어 해당 없음.
- 2026-09-20 `58637f7` — **애니메이션 4차 · 팝업 덱**(사용자: 더 적극적으로, 이전 창은 새 창의 왼쪽 뒤로, 누르면 그 팝업으로): 뒤 카드가 깊이마다 ~100px 씩 왼쪽 뒤로(폭 보정 `layoutDeck`), scrim 의 그 자리를 누르거나 왼쪽 위 스택 바 칩을 누르면 앞으로(`bringToFront`, 호버 밝아짐), 내비 표시자 슬라이드, 카드 순차 등장, 홈 행 카운트업, 펼침 높이 애니메이션, 이름표·타임라인 표식 순차 등장, Lottie 로더. 브라우저 테스트 11개.
- 2026-09-20 `c1be87b` — **애니메이션 3차(사용자: 링크 3개를 적극 활용)**: lottie_light 인라인 + 손으로 쓴 글리프 2개(브랜드 스캔 표식 · Error 없음 체크), GSAP 카운트업·막대 sweep·차트/히트맵 등장·날짜 라벨 슬라이드·무대 물러남, animate.css 토스트/칩/선택 패널. 브라우저 테스트 10개. 전후 비교 아티팩트(실제 10일치 프레임 띠 + 라이브 데모) 전달.
- 2026-09-20 `b92394b` — GSAP(Flip)·animate.css 인라인 — vendor 블록과 네트워크 가드 개정.
- 2026-09-20 `ab759f1` — **현장 10일치 검수 + 화면 요청 7건**: 현장 HTML(56,507행 · 9/10~9/20 · `--rebuild-all` 45분)은 25열·issue_codes·faults·안정 키(dev_meta.key)·dashboard_settings 전부 정상, 이관 충돌 0(superseded 0), 시각 확인 79%(장비별 아래 실물 사실). 버전 표기 v9be6d0d 는 VERSION 파일이 낡은 것 → git HEAD 우선(`updater.current_version`).
  화면: `render` 를 키 있는 morph 로(깜박임 원인 제거) + 전환 애니메이션(impeccable animate 플레이북 · **GSAP 3.15 + Flip 인라인 · animate.css 일부 인라인**, 사용자가 준 링크 3개 중 lottie-web 은 자산이 없어 제외) · 팝업 스택(아래 팝업이 뒤로 물러남, ESC 는 맨 위만) · Error 탭 유형/Job 필터 · 기간 그대로 여는 Error 팝업(여러 날) · '전체 기간 합계로' 제거 · TB500 · Kendall 두 묶음 + 열 정렬 · 수집 창 시작일 '부분' 표시(추이 평균 제외). 브라우저 테스트 9개.
- 2026-09-20 `b614aec` — **P6 답 반영(모두 권장)**: D66(이름만으로 안 합침) 기록, 호출부 없는 `_in_batch_window`·`meta.limit` 삭제(확인 7-①), 실장비 적용 순서를 다음 할 일 000 으로. 10일치 HTML 업로드 대기.
- 2026-09-20 `4bdfbac` — **P6 검토 패키지 전달**: 확인 아티팩트(단계별 변경 · 30일치 전후 · 검증/미검증 · 확인 7개 · 현장 순서) + 진행상황의 확인 대기 갱신. 코드 변경 없음. CI run 4(bf10a7b)도 초록.
- 2026-09-20 `bf10a7b` — **개선 계획 P3 수집 + P5 정리**(worktree 3개 병합 + 통합 수정): P3-A 장비 안정 키·Report 키·멱등 이관·SHA256 revision(C03) · 바뀐 이유 있을 때만 캐시 저장(C04) · 장비 색인(C09) · write_html 순차 쓰기(C07, `test_cache_identity.py` 22 · `measure_cache.py`);
  P3-B Scanresult Windows 경로 비교(C05) · 장비 확인 풀(C08) · `issue_codes` 25열/스키마 6(C12) · `utils/config.normalize_config`(C13) · `--update` 자식 프로세스·bat 회전(C14) · pythonw 안내(C16) · `meta.dashboard_settings`(D14);
  P5 archive/ 이동+gzip+SHA256SUMS(D62: 스크린샷 7 · 옛 데모 · 디자인 PROMPT/patch/시안, 옛 샘플 2장 gzip) · README/ko 정정(S03) · 화면 계약 테스트 → 제품 제약+실행 검사(S05, measure.py `--design-rules` 프로필 버그도 고침) · STATUS.md(S07) · config.example(S13) · CI progress-doc 잡(S14) · CLAUDE.md 숫자 가드(S18) · 죽은 코드 정리(C11).
  통합 중 잡은 것: 두 에이전트가 `config.example.json` 에 `read_workers` 를 각각 넣어 키가 겹침 → 하나로; 보관 정리 테스트가 소수 일수를 써 새 설정 검사(정수 1일 이상)에 걸림 → 정수 1일. CLAUDE.md 에 안정 키·issue_codes·설정 검사·C04/C07/C14/C16 절 추가. 전체 693 통과, venv 146 통과, 30일치 Chromium 오류 0.
- 2026-09-20 `ffaa1a0` — **S08 진행상황 정리(D62)**: 9/13~19 작업 기록 43항목 · 완료된 할 일 묶음(D47 이식 · 9/18 phase 0~8 · Codex 묶음 · 9/17 인계) · 해소된 확인 대기(RQ·DQ)를 `docs/history/진행상황_2026-09-13~19_기록.md` 로 원문 이동. 결정 D01~D65 는 그대로.
- 2026-09-20 `f30916a` — **개선 계획 P4 화면**: 탭 이름 'TB500 · Kendall' + 안내(D59) · 기록 없음은 살펴볼 장비와 분리(D06·D57) · Lot 선택 키에 Report(D16) · isEst/estBar/showRepeat 제거(D15) ·
  render 의 포커스·스크롤 복원 + inert + Tab 트랩 + aria-labelledby(D05) · 리포트 표 가로 스크롤 · Lot 40개 초과 시 이름표 접기(D10) · 'Error 대기'/21일 창/열람 시계 문구(D11) · 홈 품질 한 줄(D13) ·
  `meta.dashboard_settings` 덮어쓰기(D14) · 중단 분기 제거 + 평균 fault(D08, `lots[13]·[14]`) · `test_dashboard_browser.py`(S06, Chromium 4개). 30일치 Chromium: 콘솔 오류 0, 표기명 Lot 27%.
- 2026-09-20 `7d47f27` — **개선 계획 P1 보호**(worktree 에이전트 3개 병합 + 통합 수정): 업데이터(D61 CI 게이트 · SHA archive zip · zip 검사 · 허용 목록 · 저널 롤백 · TLS 검증만 · pip 상한, `test_updater.py` 대폭) ·
  수집기(D60 `--refresh-window`/`--rebuild-all`/`--full` 별칭 · 캐시 트랜잭션 + 손상 보존 C15 · CSV 잠김 부분 성공 C06 · GUI 체크, `test_collect_incremental.py`) · CI(`tests.yml` core+browser 잡, graphify 권한 분리, dev 의존성, 테스트가 저장소 파일을 안 쓰게 S04).
  통합 중 잡은 것: `collect_page` 의 계획 워커가 `deleteLater` 뒤에도 참조에 남아 창 닫을 때 qFatal(Linux CI 미러에서 실측, `test_ui_smoke` 중단) → 끝나면 참조 해제; 워커 테스트의 `cache_status` 기대값(첫 수집은 `missing`). 전체 489개 통과(CI 미러 venv).
- 2026-09-20 `5528818` — **개선 계획 P2 모델 v3**(`buildModelV3`, MODEL_VERSION 3): 장비마다 1분 축에 측정 우선 배타 배정 → 배치 창 균등 배분 → Report 끝 뒤 대기 → 자정 분할(D56·D58·D04),
  Rescan = 앞선 PASS 만(D63), Test Lot 전부 Test(D64), 수집일 분모 = 모든 장비의 마지막 기록(D57), 엄격 날짜·로더 오류 패널(D09), SETUP 파일명 Lot(D12). legacy 프로필(`buildModelLegacy`)은 디자인 동일성 가드 전용.
  30일치: fleet 59.56 → 57.24%, 대기 449 → 846h, Error 구간 169 → 302h, 불변식 위반 0(원장 Phase P2). 테스트 교체(`test_dashboard_js.py` 18 → 19, 불변식 slow 1 추가). CLAUDE.md 규칙 5·모델 절, README, 설정 도움말 갱신.
- 2026-09-20 `6d417e2` — 에이전트 worktree 폴더(`.claude/worktrees/`)가 직전 커밋에 gitlink 로 잘못 들어가 저장소에서 빼고 `.gitignore` 에 넣음.
- 2026-09-20 `037db9f` — **개선 계획 P0 기준선**: 사용자 답 11개를 D56~D65 로 기록(시간 배분 3규칙 · 수집일 분모 · 시각 없는 Error · 탭 이름 · 재수집 모드 · 배포 대상 · 보관 · Rescan · Test Lot · 진행 방식). 코드 변경 없음. 기준 SHA f3a79bd, 30일치 baseline: fleet 59.56% · Scan 10,412h · Rescan 1,224h · Error 169h · 대기 449h(원장 Phase D52·D54).
- 2026-09-20 `f3a79bd` — **전체 감사**(수집기 · 화면 · 테스트/빌드/문서 세 갈래, 30일치 실측): 개선 항목 51개를 `docs/audit/improvements_2026-09-20.json` 에 정리. 코드 변경 없음.
- 2026-09-20 `e2ab0f5` — **수집기 회귀 수정**: Job 을 끝내 못 읽은 Report(Job/Setup 없음 + 파일명·표-Lot 규칙 모두 불일치)가 `job_folder_variants` 가 빈 목록을 돌려줘 `jobs_try[0]` 에서 IndexError → Report 통째로 '읽기 실패'(10fd6a6 에서 생긴 회귀, 전체 감사에서 발견).
  이제 행은 남기고 INI 경로만 만들지 않는다(`NOT_FOUND` + data_issue). 테스트 +1.
- 2026-09-20 `4df9aaf` — **검수 답 반영 D52·D53·D54**: PASS 도 원인도 없는 Report 의 중단은 Error(유형 ABORTED, `RULES.abortIsError`) · INI 없는 Pass 행은 행마다 배치 시작~종료로 추정(`RULES.estimateFromBatch`, `t.ne`, 50% 문턱 폐지) · Test 는 부분 문자열 그대로.
  `MODEL_VERSION` 2. 30일치: fleet 54.97 → **59.61%**, Error Lot 1,719 → 2,106(+387 중단만 남은 Report), Scan 8,782 → 10,412h, 대기 548 → 449h, 추정 행 19,499(장비-일 581). 테스트 +6. D55 대로 main 에 합침.
- 2026-09-20 `4c8aa19` — **검수 버그 2: Error 팝업의 (Lot, 유형) 줄 원문** — 모델의 Lot 대표 원문은 하나라 유형이 둘인 Lot(30일치 1,597 중 175)에서 둘째 유형 줄에 남의 원문이 붙었다
  → 화면이 로드한 행에서 유형별 원문을 찾는다(`causeText`, 모델 불변). Chromium 으로 AOI-1 9/15 `PDH`(WAFER_LOST + SCAN_ERROR) 확인. 확인 항목 RQ 재번호.
- 2026-09-20 `9ecaa47` — **D47 재설계 화면 이식(P0~P5 한 커밋)**: `template.html` 을 재설계 구조(가동률 · Error · 추이 · 리포트 + 장비/Error/유형·Job 팝업, 라이트 단일)로 교체.
  모델은 `make_aoi_data.js` [원본] 블록을 `buildModel` 로 그대로 옮기고 D48 제품 유지 2건만 `RULES` 스위치(Error 후 대기 = 관측 종료까지 · 오늘 분모 = 마지막 기록까지) — 두 스위치를 끄면 30일치 장비-일 828개 출력이 스크립트와 같다(slow 가드).
  제품 기능 유지: Report 열기(장비 팝업) · 수집 상태 칩 · 수집 안 함 · 사본 저장(열·풀 그대로). 검수에서 잡은 버그 1: 오늘의 마지막 Error 뒤 대기를 관측 종료까지 셌는데 분모는 마지막 기록에서 끝나 합이 분모를 넘을 수 있었다 → 분모가 대기 끝(`we`)까지(9/18 63.43 → 62.76%).
  테스트: `test_dashboard_js.py`(18) · `test_template_contract.py`(19) 교체, `theme.py` 라이트 팔레트 동기, `measure.py` 새 모델용, README·CLAUDE.md(규칙 5 개정 · 화면 절 교체) · i18n 도움말. 30일치: fleet 40.59 → 54.97% · Error 사건 3,319 → Lot 1,719 · 대기 548h(원장 Phase D47). 확인 항목 RQ1~RQ7 아티팩트.
- 2026-09-20 `e19cede` — **디자인 추출 스크립트 편입**(`docs/design/dashboard-redesign/scripts/`, 원본 입력으로 바이트 동일 재현 확인) + 가드 `test_design_extract.py` + D47 이식 계획(P0~P5)을 다음 할 일에.
- 2026-09-20 `10fd6a6` — **4층 Job 폴더 이름 후보**(`collect.job_folder_variants`): 사용자가 NAS 에서 확인한 대로 Report `2D@RE $…_0A` ↔ 폴더 `2D@RE-$…` 라서
  원문 · 하이픈 · 접미 뗌 · 둘 다 네 이름을 정확 경로로 차례로 본다(원문에서 찾으면 그만). 30일치 4층 NOT_FOUND 2,964행 중 2,789행이 이 유형. 테스트 +3, 기존 INI 열기 횟수 기대값 갱신.
- 2026-09-20 `1a115e6` — **이식 결정 기록(D47~D51)** + 디자인 세션용 추출 스크립트 회수 프롬프트(`PROMPT_extract-script.md`) + 4층 NOT_FOUND 분석(백업이 아니라 Job 폴더 이름 `_0A/_0B` 접미 불일치 가설, 실물 확인 대기). 코드 변경 없음.

(2026-09-13 ~ 2026-09-19 의 기록 43 항목은 `docs/history/진행상황_2026-09-13~19_기록.md` 에 원문 그대로 있다.)
