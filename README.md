# AOI-capacity 코드 지도 (graphify)

main `f4569a3cafc06c231c73557df13b39e225bd5333` 기준으로 GitHub Actions 가 자동 생성. 사람이 고치지 않는다 — 다음 main 푸시 때 통째로 바뀐다.

- `graph.html` — 브라우저에서 여는 인터랙티브 그래프
- `GRAPH_TREE.html` — 커뮤니티 트리
- `GRAPH_REPORT.md` — 요약 보고서
- `graph.json` — 로컬에서 질의: `pip install graphifyy` 뒤 이 폴더를 `graphify-out/` 으로 두고 `graphify explain "parse_report()"`
