# 로컬 실행 산출물 — 데이터 폴더(%LOCALAPPDATA%\AOI_Capacity)에 생기지만, 개발 실행 시 저장소에 떨어질 수 있는 것들
VERSION
aoi_cache.json
config.json
devices.csv
collect.log
app.log
AOI_capacity.html
AOI_capacity.csv
*.bak
.update.part/
app.new/
app.new.part/
app.old/

# 빌드
dist/
build/
.venv/
*.spec.bak

# 파이썬/도구
__pycache__/
*.pyc
.pytest_cache/
.coverage

# graphify 코드 지도(생성물) — `python dev/tools/graphify_build.py` 로 다시 만든다(main 푸시마다 Actions 가 `graphify-out` 브랜치에 게시)
graphify-out/

# 디자인 프로토타입의 생성물(dev/tools/design_bundle.py 가 다시 만든다)
docs/design/**/AOI 가동 현황*.html

# 세션 에이전트 worktree(저장소에 넣지 않는다)
.claude/worktrees/
